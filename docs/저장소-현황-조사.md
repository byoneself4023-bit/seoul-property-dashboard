# 저장소 현황 조사 — ~/Project

- 조사일: 2026-08-12
- 조사 범위: `~/Project` 전체 (`node_modules/`, `.git/` 내부 제외)
- 조사 방식: 읽기 전용. 파일 삭제·이동·수정 없음. RTMS API에 대해 유형별 1건씩 총 4회 GET 호출(읽기)만 수행.
- 표기 규칙: `[확인]` = 실측/원문 대조 완료, `[미확인]` = 이번 조사에서 검증하지 못함
- 경로 표기: 2026-08-13 공개 저장소 전환 시 절대경로 `/Users/<사용자>/`를 `~/`로
  일괄 치환했다. 아래 plist·로그 **인용문 안의 경로도 마찬가지로 치환된 값**이며,
  실제 파일에는 절대경로가 들어 있다. 그 외 내용은 원문 그대로다.

---

## 1. 저장소 전체 구조

### 1.1 최상위 엔트리

`ls -la ~/Project` 실측 (2026-08-12):

| 경로 | 종류 | 실측 용량 | 파일 수 | 역할 |
|---|---|---:|---:|---|
| `~/Project/.claude/` | dir | 4.9M | — | Claude Code 설정. `skills/` 4.2M, `rules/` 272K, `agents/` 176K, `hooks/` 108K, `commands/` 60K, `output-styles/` 32K |
| `~/Project/.git/` | dir | 51M | — | git 저장소 본체 (§1.3) |
| `~/Project/.moai/` | dir | 548K | 57 tracked | MoAI-ADK 상태·플랜·리포트·브랜드/DB 문서 |
| `~/Project/dashboard/` | dir | 41M | 5,514 | **대시보드 제품 본체** (§3, §4) |
| `~/Project/drive-docs/` | dir | 44M | 35 tracked | 외부 문서 원본(docx/pdf/pptx) + `md/` 변환본 19개 |
| `~/Project/newsletter/` | dir | 56M | 2 | **로그 파일 2개만 남은 잔재** (§2) |
| `~/Project/node_modules/` | dir | 29M | — | `puppeteer` 개발 의존성 (gitignored) |
| `~/Project/docs/` | dir | 8.0K | 1 | `전체플랫폼_스택제안.md` 1개 |
| `~/Project/__pycache__/` | dir | 8.0K | 1 | `.scratch_reflow.cpython-312.pyc` — 대응 `.py` 소스 없음. 고아 산출물 |
| `~/Project/CLAUDE.md` | file | 28,077 B | — | MoAI 실행 지시서 (§6.2) |
| `~/Project/CHANGELOG.md` | file | 1,038 B | — | 변경 이력 (§6.3에 불일치 지적) |
| `~/Project/.gitignore` | file | 4,589 B | — | MoAI 배포 템플릿 기반 |
| `~/Project/package.json` | file | 348 B | — | `name: seoul-property-dashboard`, `scripts.verify`, devDep `puppeteer ^25.3.0` |
| `~/Project/package-lock.json` | file | 12,037 B | — | 위 lockfile |
| `~/Project/.env` | file | 208 B | — | `RTMS_SERVICE_KEY`, `ANTHROPIC_API_KEY` 2개 키. gitignored `[확인]` |
| `~/Project/.env.example` | file | 178 B | — | `RTMS_SERVICE_KEY=` 만 |
| `~/Project/.mcp.json` | file | 868 B | — | MCP 서버 설정 |
| `~/Project/.DS_Store` | file | 10,244 B | — | macOS 산출물 |

### 1.2 dashboard/ 내부

| 경로 | 실측 바이트 | 비고 |
|---|---:|---|
| `~/Project/dashboard/dashboard.html` | 748,573 | git tracked. 단일 파일 산출물 |
| `~/Project/dashboard/data.json` | 469,767 | **gitignored** (`.gitignore:225`) |
| `~/Project/dashboard/ingest.mjs` | 65,867 (1,418 lines) | 수집 파이프라인 전체 |
| `~/Project/dashboard/verify-quality.mjs` | 2,978 | puppeteer 품질 게이트 |
| `~/Project/dashboard/.cache/ingest/` | 27,396,306 (5,500 files) | **gitignored** (`.gitignore:230`) |
| `~/Project/dashboard/reference/dashboard.html` | 45,868 | 초기 프로토타입 보존본 |
| `~/Project/dashboard/scripts/run-ingest.sh` | 1,783 | launchd 래퍼 |
| `~/Project/dashboard/scripts/deploy.sh` | 4,535 | GitHub Pages 배포 |
| `~/Project/dashboard/scripts/install-scheduler.sh` | 2,548 | LaunchAgent 등록 |
| `~/Project/dashboard/scripts/uninstall-scheduler.sh` | 437 | LaunchAgent 해제 |
| `~/Project/dashboard/docs/실데이터_연동_가이드.md` | 13,445 | 유일한 제품 문서 |
| `~/Project/dashboard/logs/ingest-launchd.log` | 4,331 | 최신 실행 로그 |
| `~/Project/dashboard/logs/ingest-launchd.log.prev` | 3,956 | 직전 1세대 |
| `~/Project/dashboard/logs/launchd-stderr.log` | 0 | 비어 있음 |
| `~/Project/dashboard/.claude/settings.local.json` | 95 | — |

`du -sh` 기준 `.cache` 39M (블록 단위), 실 바이트 합계 27,396,306. 파일 수가 많아 블록 오버헤드가 큼.

### 1.3 git 상태

`[확인]` git 저장소는 **루트 1개뿐**. `find . -name .git -maxdepth 4 -not -path "./node_modules/*"` → `./.git` 만 반환.

```
branch: main (로컬 유일, 원격 브랜치 없음)
git remote -v: (출력 없음 — 원격 미설정)
git status --short: (0줄 — 클린)
```

마지막 커밋 5개:

```
c36af8a 2026-07-20 chore(moai): gitignore session runtime telemetry
7e38fd9 2026-07-20 chore(moai): gitignore .moai/state session runtime
93fbdce 2026-07-20 docs(moai): 인제스트 플랜 기록
d703e28 2026-07-20 chore(dashboard): 주간 데이터 리프레시 2026-07-13
9832639 2026-07-20 docs(dashboard): update monorepo guide after newsletter removal
```

`git count-objects -vH` 실측:

```
count: 1260
size: 50.70 MiB
in-pack: 0
packs: 0
size-pack: 0 bytes
```

`[확인]` **loose object 1,260개, pack 0개.** 한 번도 `git gc`가 돌지 않았다. `dashboard/dashboard.html` blob 이력:

| commit | date | blob bytes |
|---|---|---:|
| `d703e28` | 2026-07-20 | 748,573 |
| `b802893` | 2026-07-13 | 748,730 |
| `1001204` | 2026-07-13 | 528,177 |
| `40ad458` | 2026-07-12 | 528,106 |
| `75a9a91` | 2026-07-12 | 527,480 |

데이터 인라인 모델이므로 수집 1회 = 새 750KB blob 1개.

---

## 2. 뉴스레터 잔재

### 2.1 남아 있는 실체

```
~/Project/newsletter/
└── logs/                                       (drwxr--r--, Jul 21 10:02)
    ├── scheduler.log      40,716 bytes   mtime 2026-08-12 09:48
    └── ui-server.log  58,484,440 bytes   mtime 2026-08-12 12:59
```

`[확인]` 디렉터리 2개, 파일 2개. 코드·설정·테스트는 전혀 없음.

`ui-server.log` 원문 (head -5 / tail -5):

```
node:internal/modules/cjs/loader:1423
  throw err;
  ^

Error: Cannot find module '~/Project/newsletter/ui/server.mjs'
...
  code: 'MODULE_NOT_FOUND',
  requireStack: []
}

Node.js v25.1.0
```

`scheduler.log` 원문 (head -10):

```
node:internal/modules/cjs/loader:1423
  throw err;
  ^

Error: Cannot find module '~/Project/newsletter/scripts/scheduler-run.mjs'
    at Module._resolveFilename (node:internal/modules/cjs/loader:1420:15)
    at defaultResolveImpl (node:internal/modules/cjs/loader:1058:19)
    at resolveForCJSWithHooks (node:internal/modules/cjs/loader:1063:22)
    at Module._load (node:internal/modules/cjs/loader:1226:37)
    at TracingChannel.traceSync (node:diagnostics_channel:328:14)
```

`grep -c MODULE_NOT_FOUND` 실측:

- `ui-server.log` → **78,850회**
- `scheduler.log` → **54회**

### 2.2 원인 — 미제거 LaunchAgent 2개

`[확인]` 코드는 커밋 `1d3d7c3` (2026-07-20 13:38:25 +0900, `chore: remove newsletter project (failed experiment)`, 60 files changed / 7,045 deletions)로 삭제되었으나, **LaunchAgent 2개는 해제되지 않았다.**

`~/Library/LaunchAgents/com.newsletter.ui.plist` 원문:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.newsletter.ui</string>

  <key>ProgramArguments</key>
  <array>
    <string>/usr/local/Cellar/node/25.1.0_1/bin/node</string>
    <string>~/Project/newsletter/ui/server.mjs</string>
    <string>--port</string>
    <string>3456</string>
  </array>

  <!-- 로그인 시 자동 기동 -->
  <key>RunAtLoad</key>
  <true/>

  <!-- 크래시/종료 시 자동 재기동 — 상시 서비스 유지 -->
  <key>KeepAlive</key>
  <true/>

  <key>StandardOutPath</key>
  <string>~/Project/newsletter/logs/ui-server.log</string>
  <key>StandardErrorPath</key>
  <string>~/Project/newsletter/logs/ui-server.log</string>
</dict>
</plist>
```

`~/Library/LaunchAgents/com.newsletter.daily.plist` 원문:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.newsletter.daily</string>

  <key>ProgramArguments</key>
  <array>
    <string>/usr/local/Cellar/node/25.1.0_1/bin/node</string>
    <string>~/Project/newsletter/scripts/scheduler-run.mjs</string>
  </array>

  <!-- 매일 KST 07:00 = UTC 22:00(전일) 트리거 -->
  <!-- [HARD] 일간 전용: 주간/월간 자동 트리거 없음 (REQ-SCHED-004, EXC-212) -->
  <key>StartCalendarInterval</key>
  <dict>
    <key>Hour</key>
    <integer>22</integer>
    <key>Minute</key>
    <integer>0</integer>
  </dict>

  <key>StandardOutPath</key>
  <string>~/Project/newsletter/logs/scheduler.log</string>
  <key>StandardErrorPath</key>
  <string>~/Project/newsletter/logs/scheduler.log</string>

  <!-- 로그인(에이전트 로드) 시에도 실행 — 밤에 맥 완전 종료 시 07:00 정시 catch-up 누락 보완 -->
  <!-- 중복 실행은 scheduler-run.mjs의 하루 1회 가드가 방지 (REQ-SCHED-005) -->
  <key>RunAtLoad</key>
  <true/>
</dict>
</plist>
```

`launchctl list | grep -iE "newsletter|kuka"` 실측:

```
-	1	com.newsletter.daily
-	0	com.kuka.dashboard-ingest
-	1	com.newsletter.ui
```

`launchctl print gui/501/com.newsletter.ui` 발췌:

```
	state = spawn scheduled
	runs = 1157
	last exit code = 1
```

`[확인]` `KeepAlive=true` + 실행 파일 부재 → launchd 최소 실행간격(10초)에 걸린 무한 재기동 루프. 현재 로그인 세션에서만 1,157회 재시작. `ui-server.log` mtime이 조사 시점(12:59)과 일치 → **지금도 계속 증가 중**.

`[확인]` `com.newsletter.daily`는 UTC 22:00 = KST 07:00 1일 1회 + `RunAtLoad`. 누적 54회.

### 2.3 코드 참조 검색

`grep -rn -i "newsletter"` (`node_modules`, `.git`, `logs`, `.cache` 제외) 결과 — **총 9건, 전부 무관**:

```
.claude/skills/moai-library-shadcn/modules/shadcn-components.md:181: newsletter: z.boolean(),
.claude/skills/moai-library-shadcn/modules/shadcn-components.md:210: newsletter: false,
.claude/skills/moai-library-shadcn/references/examples.md:444: newsletter: false,
.claude/skills/moai-library-shadcn/references/examples.md:540: id="newsletter"
.claude/skills/moai-library-shadcn/references/examples.md:541: checked={formData.newsletter}
.claude/skills/moai-library-shadcn/references/examples.md:543: setFormData({ ...formData, newsletter: checked })
.claude/skills/moai-library-shadcn/references/examples.md:546: <label htmlFor="newsletter" className="text-sm cursor-pointer">
.claude/skills/moai-library-shadcn/references/examples.md:547: Subscribe to newsletter
.claude/skills/moai-domain-uiux/modules/web-interface-guidelines.md:168:  <span>Subscribe to newsletter</span>
```

전부 shadcn/uiux 스킬 문서의 폼 예제. 이 저장소 제품 코드와 무관.

`grep -rn "뉴스레터"` 결과 — 4개 파일:

```
.moai/plans/cozy-gliding-lightning.md:36:- 과제 2 뉴스레터(찐소장) — `drive-docs/md/데일리_부동산_뉴스레터_기획안.md` 기반 스코프 착수.
docs/전체플랫폼_스택제안.md:16:  ... 데일리 부동산 뉴스레터 "찐소장"(과제 2).
docs/전체플랫폼_스택제안.md:28,37: (스택 제안 표 내 언급)
drive-docs/md/데일리_부동산_뉴스레터_기획안.md: (기획 문서 본문 전체)
```

`[확인]` **실행 코드 참조 0건.** 기획/계획 문서 언급만 존재.

### 2.4 git 추적 상태

```
$ git check-ignore -v newsletter/logs
.gitignore:107:logs/	newsletter/logs
$ git check-ignore -v newsletter/logs/ui-server.log
.gitignore:107:logs/	newsletter/logs/ui-server.log
$ git check-ignore -v newsletter/logs/scheduler.log
.gitignore:107:logs/	newsletter/logs/scheduler.log
$ git ls-files newsletter | wc -l
0
```

`[확인]` `.gitignore:107` 의 `logs/` 패턴에 걸려 3개 경로 모두 무시됨. 추적 파일 0개. **삭제해도 git 이력·워킹트리에 아무 영향 없음.**

`git log --oneline --all -- newsletter` → 13개 커밋 (`1d3d7c3` 삭제 커밋 포함). 이력은 보존되어 있으므로 복구 필요 시 `git show 1d3d7c3^:newsletter/...` 로 접근 가능.

### 2.5 처리 판단

| 대상 | 판단 | 근거 |
|---|---|---|
| `~/Project/newsletter/` 디렉터리 전체 (58.5MB) | **지워도 됨** | git 미추적 + `.gitignore:107` 무시 + 코드 참조 0건 + 내용이 MODULE_NOT_FOUND 스택트레이스뿐 |
| `~/Library/LaunchAgents/com.newsletter.ui.plist` | **해제 필요** (선행 조건) | `KeepAlive=true`. plist를 남긴 채 폴더만 지우면 launchd가 로그 폴더를 다시 만들고 루프 지속 |
| `~/Library/LaunchAgents/com.newsletter.daily.plist` | **해제 필요** (선행 조건) | 위와 동일. 1일 1회 |
| `.moai/plans/cozy-gliding-lightning.md:36` | **확인 필요** | 과제 2 착수 계획 기록. 이력 문서로 남길지 판단 필요 |
| `docs/전체플랫폼_스택제안.md:16,28,37` | **확인 필요** | 플랫폼 전체 스택 제안서. 뉴스레터가 제품 목록에 남아 있음 |
| `drive-docs/md/데일리_부동산_뉴스레터_기획안.md` (15,885 B) 및 `drive-docs/데일리_부동산_뉴스레터_기획안.pdf` (218,482 B) | **보존** | 외부 기획 원본. 코드 잔재가 아니라 문서 자산 |
| `.claude/skills/**` 의 `newsletter` 문자열 | **보존** | 서드파티 스킬 예제 코드. 이 제품과 무관 |

`[미확인]` 두 LaunchAgent의 로그 증가 속도(바이트/일)는 로그 로테이션 이력이 없어 산출하지 못함. 파일 mtime과 누적 78,850회만 확인.

---

## 3. 데이터 수집 파이프라인

### 3.1 API 목록

`~/Project/dashboard/ingest.mjs:88-93`:

```javascript
const ENDPOINTS = {
  apt:  'http://apis.data.go.kr/1613000/RTMSDataSvcAptTradeDev/getRTMSDataSvcAptTradeDev',
  rh:   'http://apis.data.go.kr/1613000/RTMSDataSvcRHTrade/getRTMSDataSvcRHTrade',
  sh:   'http://apis.data.go.kr/1613000/RTMSDataSvcSHTrade/getRTMSDataSvcSHTrade',
  offi: 'http://apis.data.go.kr/1613000/RTMSDataSvcOffiTrade/getRTMSDataSvcOffiTrade', // 오피스텔 — 제외하려면 이 줄 주석 처리
};
```

| 키 | 정식 명칭 | provider | 프로토콜 |
|---|---|---|---|
| `apt` | 국토교통부_아파트 매매 실거래가 상세 자료 | 1613000 | **http (평문)** |
| `rh` | 국토교통부_연립다세대 매매 실거래가 자료 | 1613000 | **http (평문)** |
| `sh` | 국토교통부_단독/다가구 매매 실거래가 자료 | 1613000 | **http (평문)** |
| `offi` | 국토교통부_오피스텔 매매 신고 조회 | 1613000 | **http (평문)** |

`[확인]` 4종 전부 **매매(Trade)** 전용. 전월세 API는 호출하지 않는다.

요청 파라미터 — `ingest.mjs:516-523`:

```javascript
async function fetchOnePage(serviceKey, endpoint, lawdCd, dealYmd, pageNo) {
  const url = new URL(endpoint);
  url.searchParams.set('serviceKey', serviceKey);
  url.searchParams.set('LAWD_CD',    lawdCd);
  url.searchParams.set('DEAL_YMD',   dealYmd);
  url.searchParams.set('pageNo',     String(pageNo));
  url.searchParams.set('numOfRows',  String(PAGE_SIZE));
```

관련 상수 — `ingest.mjs:39-49`:

```javascript
const DELAY_MS  = 200;   // 요청 간 대기 — 일일 한도(10,000건) 방어
const PAGE_SIZE = 1000;  // numOfRows
const MAX_RETRY = 3;     // 지수 백오프 최대 재시도
const CACHE_DIR = join(__dirname, '.cache', 'ingest');

// 공간 규격 버킷 경계 (전용면적 m²) — 원룸형 ≤40 / 투룸형 40<x≤60 / 쓰리룸+ >60
const AREA_BUCKETS = { studioMax: 40, twoMax: 60 };
// 금액대 버킷 경계 (거래금액 만원) — 3억↓ ≤30000 / 3~6억 30000<x≤60000 / 6억↑ >60000
const PRICE_BUCKETS = { under3Max: 30000, under6Max: 60000 };
// 월간 집계 시작 (YYYYMM) — 연도 탐색을 위해 2022-01부터 수집
const START_YM = '202201';
```

수집 대상 구 목록은 `ingest.mjs:56-79` `DISTRICTS` 배열 (서울 25개 자치구 `{ name, code }`).

### 3.2 API 실제 응답 원문 (2026-08-12 실측, `LAWD_CD=11110&DEAL_YMD=202605&pageNo=1&numOfRows=1`)

**apt** — `RTMSDataSvcAptTradeDev`:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?><response><header><resultCode>000</resultCode><resultMsg>OK</resultMsg></header><body><items><item><aptDong> </aptDong><aptNm>아남1</aptNm><aptSeq>11110-25</aptSeq><bonbun>0004</bonbun><bubun>0000</bubun><buildYear>1995</buildYear><buyerGbn>개인</buyerGbn><cdealDay> </cdealDay><cdealType> </cdealType><dealAmount>115,000</dealAmount><dealDay>31</dealDay><dealMonth>5</dealMonth><dealYear>2026</dealYear><dealingGbn>중개거래</dealingGbn><estateAgentSggNm>서울 종로구</estateAgentSggNm><excluUseAr>84.9</excluUseAr><floor>13</floor><jibun>4</jibun><landCd>1</landCd><landLeaseholdGbn>N</landLeaseholdGbn><rgstDate> </rgstDate><roadNm>창경궁로</roadNm><roadNmBonbun>00265</roadNmBonbun><roadNmBubun>00000</roadNmBubun><roadNmCd>3005008</roadNmCd><roadNmSeq>07</roadNmSeq><roadNmSggCd>11110</roadNmSggCd><roadNmbCd>0</roadNmbCd><sggCd>11110</sggCd><slerGbn>개인</slerGbn><umdCd>17100</umdCd><umdNm>명륜2가</umdNm></item></items><numOfRows>1</numOfRows><pageNo>1</pageNo><totalCount>89</totalCount></body></response>
```

**rh** — `RTMSDataSvcRHTrade`:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?><response><header><resultCode>000</resultCode><resultMsg>OK</resultMsg></header><body><items><item><buildYear>1990</buildYear><buyerGbn>개인</buyerGbn><cdealDay> </cdealDay><cdealType> </cdealType><dealAmount>39,500</dealAmount><dealDay>26</dealDay><dealMonth>5</dealMonth><dealYear>2026</dealYear><dealingGbn>중개거래</dealingGbn><estateAgentSggNm>서울 종로구</estateAgentSggNm><excluUseAr>57.33</excluUseAr><floor>3</floor><houseType>연립</houseType><jibun>577</jibun><landAr>56.32</landAr><mhouseNm>정원파크빌라</mhouseNm><rgstDate> </rgstDate><sggCd>11110</sggCd><slerGbn>개인</slerGbn><umdNm>숭인동</umdNm></item></items><numOfRows>1</numOfRows><pageNo>1</pageNo><totalCount>59</totalCount></body></response>
```

**sh** — `RTMSDataSvcSHTrade`:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?><response><header><resultCode>000</resultCode><resultMsg>OK</resultMsg></header><body><items><item><buildYear>2008</buildYear><buyerGbn>법인</buyerGbn><cdealDay> </cdealDay><cdealType> </cdealType><dealAmount>276,000</dealAmount><dealDay>6</dealDay><dealMonth>5</dealMonth><dealYear>2026</dealYear><dealingGbn>중개거래</dealingGbn><estateAgentSggNm>서울 강남구</estateAgentSggNm><houseType>단독</houseType><jibun>1*</jibun><plottageAr>215.9</plottageAr><sggCd>11110</sggCd><slerGbn>개인</slerGbn><totalFloorAr>181.15</totalFloorAr><umdNm>가회동</umdNm></item></items><numOfRows>1</numOfRows><pageNo>1</pageNo><totalCount>25</totalCount></body></response>
```

**offi** — `RTMSDataSvcOffiTrade`:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?><response><header><resultCode>000</resultCode><resultMsg>OK</resultMsg></header><body><items><item><buildYear>2007</buildYear><buyerGbn>개인</buyerGbn><cdealDay> </cdealDay><cdealType> </cdealType><dealAmount>49,000</dealAmount><dealDay>22</dealDay><dealMonth>5</dealMonth><dealYear>2026</dealYear><dealingGbn>중개거래</dealingGbn><estateAgentSggNm>서울 종로구</estateAgentSggNm><excluUseAr>56.64</excluUseAr><floor>20</floor><jibun>24</jibun><offiNm>르메이에르종로타운1</offiNm><sggCd>11110</sggCd><sggNm>종로구</sggNm><slerGbn>법인</slerGbn><umdNm>종로1가</umdNm></item></items><numOfRows>1</numOfRows><pageNo>1</pageNo><totalCount>28</totalCount></body></response>
```

### 3.3 저장 필드 vs 폐기 필드

파서 — `~/Project/dashboard/ingest.mjs:223-260` 전문:

```javascript
export function parseItems(xml) {
  checkResultCode(xml);

  const items = [];
  const itemRe = /<item>([\s\S]*?)<\/item>/g;
  let itemMatch;
  while ((itemMatch = itemRe.exec(xml)) !== null) {
    const block = itemMatch[1];
    // data.go.kr 상세(Dev) 엔드포인트는 영문 태그(dealYear/dealMonth/dealDay),
    // 구형 엔드포인트는 한글 태그(년/월/일) — 둘 다 지원
    const yearMatch  = block.match(/<dealYear>\s*(\d+)\s*<\/dealYear>/)   || block.match(/<년>\s*(\d+)\s*<\/년>/);
    const monthMatch = block.match(/<dealMonth>\s*(\d+)\s*<\/dealMonth>/) || block.match(/<월>\s*(\d+)\s*<\/월>/);
    const dayMatch   = block.match(/<dealDay>\s*(\d+)\s*<\/dealDay>/)     || block.match(/<일>\s*(\d+)\s*<\/일>/);
    if (!yearMatch || !monthMatch) continue;

    // 전용면적 파싱: 영문 태그 <excluUseAr> 또는 한글 태그 <전용면적>
    const areaMatch = block.match(/<excluUseAr>\s*([\d.]+)\s*<\/excluUseAr>/) ||
                      block.match(/<전용면적>\s*([\d.]+)\s*<\/전용면적>/);
    const area = areaMatch ? parseFloat(areaMatch[1]) : null;

    // 거래금액 파싱: 영문 태그 <dealAmount> 또는 한글 태그 <거래금액>
    // 값에 쉼표와 공백이 포함될 수 있음 (예: "  82,500") — 제거 후 정수 변환
    const amountMatch = block.match(/<dealAmount>\s*([\d,\s]+)\s*<\/dealAmount>/) ||
                        block.match(/<거래금액>\s*([\d,\s]+)\s*<\/거래금액>/);
    const amount = amountMatch
      ? parseInt(amountMatch[1].replace(/[,\s]/g, ''), 10)
      : null;

    items.push({
      year:   parseInt(yearMatch[1],  10),
      month:  parseInt(monthMatch[1], 10),
      day:    dayMatch ? parseInt(dayMatch[1], 10) : null,
      area:   (area !== null && !isNaN(area))     ? area   : null,
      amount: (amount !== null && !isNaN(amount)) ? amount : null,
    });
  }
  return items;
}
```

**저장 필드 (5개)**: `dealYear` → `year`, `dealMonth` → `month`, `dealDay` → `day`, `excluUseAr` → `area`, `dealAmount` → `amount`

**폐기 필드 (apt 기준 27개)**: `aptDong`, `aptNm`, `aptSeq`, `bonbun`, `bubun`, `buildYear`, `buyerGbn`, `cdealDay`, `cdealType`, `dealingGbn`, `estateAgentSggNm`, `floor`, `jibun`, `landCd`, `landLeaseholdGbn`, `rgstDate`, `roadNm`, `roadNmBonbun`, `roadNmBubun`, `roadNmCd`, `roadNmSeq`, `roadNmSggCd`, `roadNmbCd`, `sggCd`, `slerGbn`, `umdCd`, `umdNm`

**유형별 추가 폐기 필드**: rh — `houseType`, `landAr`, `mhouseNm` / sh — `houseType`, `plottageAr`, `totalFloorAr` / offi — `offiNm`, `sggNm`

### 3.4 질문 항목 직답

| 질문 | 답 | 근거 |
|---|---|---|
| **거래금액을 저장하고 있는가** | **저장한다.** `.cache/ingest/*.json` 각 item의 `amount` 필드 (단위: 만원, 정수) | `ingest.mjs:245-249, 256` / 캐시 전수 스캔 결과 amount null = 0건 |
| 단지명 (`aptNm`/`mhouseNm`/`offiNm`) | **저장 안 함 (폐기)** | `parseItems`에 추출 로직 없음 |
| 법정동 (`umdNm`/`umdCd`) | **저장 안 함 (폐기)** | 동일. 구(`LAWD_CD`) 단위까지만 식별 가능 |
| 전용면적 (`excluUseAr`) | **저장한다.** 단 sh(단독/다가구)는 API가 필드 자체를 주지 않아 100% null | `ingest.mjs:239-241` / §3.5 표 |
| 층 (`floor`) | **저장 안 함 (폐기)** | 동일 |
| 계약일 (`dealYear/Month/Day`) | **저장한다.** 일 단위까지 | `ingest.mjs:252-254` |
| 건축년도 (`buildYear`) | **저장 안 함 (폐기)** | 동일 |
| 거래유형 (`dealingGbn`), 매수·매도자 구분 (`buyerGbn`/`slerGbn`) | **저장 안 함 (폐기)** | 동일 |
| 원본을 버리고 집계만 남기는가 | **캐시 계층은 거래 단위 원본을 보존한다.** 집계는 별도로 `data.json`에 생성 | §3.6 |

### 3.5 캐시 실측 (2026-08-12, 5,500 파일 전수 파싱)

`schemaVersion` 분포: `"schemaVersion":2` **5,500개 (100%)**. v1 잔존 0건. `items` 빈 파일 30개.

| propType | 레코드 수 | area null | amount null | day null |
|---|---:|---:|---:|---:|
| `apt` | 229,980 | 0 (0.0%) | 0 | 0 |
| `rh` | 140,727 | 0 (0.0%) | 0 | 0 |
| `offi` | 55,627 | 0 (0.0%) | 0 | 0 |
| `sh` | 17,029 | **17,029 (100.0%)** | 0 | 0 |
| **합계** | **443,363** | 17,029 | 0 | 0 |

`[확인]` sh의 area 전량 null은 API 스펙 자체가 원인. §3.2 sh 응답에 `excluUseAr` 태그가 없고 `plottageAr`(대지면적) / `totalFloorAr`(연면적)만 존재. 이 사실은 UI에 이미 반영되어 있음 — `dashboard.html:176`:

```html
  <p id="chartNote" class="hidden mt-2 text-[11px] text-slate-500">※ 단독/다가구는 전용면적 미제공으로 공간규격 집계에서 제외됩니다.</p>
```

거래금액 분포 (443,363건, 단위 만원): min **2,000** / median **57,000** / max **11,087,780**

### 3.6 저장 위치와 형식 (2계층)

**계층 1 — 원본 캐시**

- 경로: `~/Project/dashboard/.cache/ingest/{LAWD_CD}_{YYYYMM}_{propType}.json`
- 형식: 1줄 JSON. 스키마 `{ schemaVersion: 2, items: [...], totalCount: number }`
- 파일 수: 5,500 = 25구 × 55개월 × 4유형
- 실측 합계: **27,396,306 bytes** (레코드당 평균 61 bytes)
- git: **미추적** (`.gitignore:230` `.cache/`)

실제 파일 원문 발췌 — `~/Project/dashboard/.cache/ingest/11110_202605_apt.json` (5,467 bytes) 선두:

```json
{"schemaVersion":2,"items":[{"year":2026,"month":5,"day":31,"area":84.9,"amount":115000},{"year":2026,"month":5,"day":30,"area":59.01,"amount":59000},{"year":2026,"month":5,"day":25,"area":110.53,"amount":111000},{"year":2026,"month":5,"day":30,"area":84.9,"amount":104000},{"year":2026,"month":5,"day":1,"area":37.2635,"amount":100000},
```

저장 로직 — `ingest.mjs:285-289`:

```javascript
function cacheSave(lawdCd, dealYmd, propType, data) {
  mkdirSync(CACHE_DIR, { recursive: true });
  // schemaVersion: 2 를 함께 저장해 구버전 캐시(v1)와 구분
  writeFileSync(cachePath(lawdCd, dealYmd, propType), JSON.stringify({ schemaVersion: 2, ...data }), 'utf8');
}
```

캐시 유효성 판정 — `ingest.mjs:553-563`:

```javascript
async function fetchCombo(serviceKey, endpoint, lawdCd, dealYmd, propType, useCache, stats) {
  // 캐시 확인: schemaVersion === 2 인 경우에만 유효한 캐시로 처리
  // v1 캐시(날짜만 있고 area/amount 없음)는 자동 무효화 → 재수집
  if (useCache) {
    const cached = cacheLoad(lawdCd, dealYmd, propType);
    if (cached !== null && cached.schemaVersion === 2) {
      stats.hits++;
      stats.hitsByType[propType]++;
      return cached;
    }
  }
```

**계층 2 — 집계 산출물**

- 경로: `~/Project/dashboard/data.json` (469,767 bytes, gitignored `.gitignore:225`)
- 동일 JSON이 `~/Project/dashboard/dashboard.html` 의 `<script type="application/json" id="real-data">` 슬롯(187~23,515행)에 인라인 주입됨

`data.json` 최상위 구조 실측:

```
schemaVersion: 2
generatedAt:   "2026-07-13"
source:        "rtms"
periods.week:  12개  (예: "2026-04-20~2026-04-26")
periods.month: 54개  (예: "2022-01")
byDistrict:    25개 구
```

구·기간 1셀의 실제 형태 — `byDistrict["종로구"].month[53]`:

```json
{
 "apt": 23,
 "nonApt": 86,
 "room": {
  "studio": 33,
  "two": 31,
  "three": 38
 },
 "price": {
  "under3": 30,
  "under6": 33,
  "over6": 23
 }
}
```

`[확인]` **집계 계층에는 금액 원값이 남지 않는다.** `amount`는 3구간 버킷 카운트(`under3`/`under6`/`over6`)로만 환원되고, 평균·중앙값·총액·㎡당 단가는 어디에도 계산·저장되지 않는다.

집계 로직 — `ingest.mjs:342-348` (버킷 판정):

```javascript
  // 금액 버킷 결정 헬퍼
  function priceBucket(amount) {
    if (amount === null) return null;
    if (amount <= PRICE_BUCKETS.under3Max) return 'under3';
    if (amount <= PRICE_BUCKETS.under6Max) return 'under6';
    return 'over6';
  }
```

`ingest.mjs:425-445` (구별 조립, 월간 부분):

```javascript
    // 면적별 합산 헬퍼 (apt + rh + sh + offi 전체)
    // @MX:NOTE: [AUTO] room은 모든 유형(아파트 포함) 면적 합산, price는 비아파트(rh+sh+offi)만
    const sumArr = (arrs) => arrs[0].map((_, i) => arrs.reduce((s, a) => s + a[i], 0));

    byDistrict[name] = {
      month: monthPeriods.map((_, i) => ({
        apt:    aptAgg.monthly[i],
        nonApt: rhAgg.monthly[i] + shAgg.monthly[i] + offiAgg.monthly[i],
        // 공간규격별: apt + rh + sh + offi 전체 면적 버킷 합산
        room: {
          studio: aptAgg.monthlyArea.studio[i] + rhAgg.monthlyArea.studio[i] + shAgg.monthlyArea.studio[i] + offiAgg.monthlyArea.studio[i],
          two:    aptAgg.monthlyArea.two[i]    + rhAgg.monthlyArea.two[i]    + shAgg.monthlyArea.two[i]    + offiAgg.monthlyArea.two[i],
          three:  aptAgg.monthlyArea.three[i]  + rhAgg.monthlyArea.three[i]  + shAgg.monthlyArea.three[i]  + offiAgg.monthlyArea.three[i],
        },
        // 금액대별: 비아파트(rh + sh + offi)만 집계 — 레이블 "금액대별(비아파트)"
        price: {
          under3: rhAgg.monthlyPrice.under3[i] + shAgg.monthlyPrice.under3[i] + offiAgg.monthlyPrice.under3[i],
          under6: rhAgg.monthlyPrice.under6[i] + shAgg.monthlyPrice.under6[i] + offiAgg.monthlyPrice.under6[i],
          over6:  rhAgg.monthlyPrice.over6[i]  + shAgg.monthlyPrice.over6[i]  + offiAgg.monthlyPrice.over6[i],
        },
      })),
```

`[확인]` **`price` 버킷은 아파트(`aptAgg`)를 포함하지 않는다.** `rhAgg + shAgg + offiAgg` 만 합산. 즉 현재 화면의 금액대 차트는 비아파트 전용이며, 아파트 229,980건(전체의 51.9%)의 금액 정보는 집계 계층에 전혀 반영되지 않는다.

`sumArr` (`ingest.mjs:427`)는 정의되어 있으나 이후 코드에서 호출되지 않는 **미사용 헬퍼**.

### 3.7 데이터 보유 기간

캐시 443,363건 전수 스캔:

```
최초 거래일: 2022-01-01
최종 거래일: 2026-07-11
월 범위:    2022-01 ~ 2026-07 (55개월)
```

최근 6개월 건수:

```
2026-02: 10,433
2026-03: 11,195
2026-04: 15,129
2026-05: 14,581
2026-06:  8,242
2026-07:  1,002
```

`[확인]` 2026-06, 2026-07이 급감한 것은 마지막 수집(2026-07-13) 이후 갱신이 없어서다. 신고 지연(최대 30일)이 반영되지 않은 상태로 고정되어 있다. §5.3 참조.

---

## 4. 화면 렌더링 구조

### 4.1 `dashboard.html` 구성 (24,517 lines / 748,573 bytes)

| 구간 | 행 범위 | 실측 bytes | 비중 | 내용 |
|---|---|---:|---:|---|
| 문서 머리말 | 1–7 | 326 | 0.0% | `<!DOCTYPE>`, `<html lang="ko">`, meta, `<title>서울 부동산 거래량 대시보드</title>` |
| CSS | 8–10 | 11,992 | 1.6% | Tailwind **정적 빌드 결과물**을 `<style>`에 인라인 |
| head `<script>` | 11–27 | 208,545 | 27.9% | **Chart.js v4.5.1 전문 인라인** |
| 화면 마크업 | 29–186 | 12,197 | 1.6% | header + section 5개 |
| `#real-data` | 187–23,515 | **469,823** | **62.8%** | `data.json` 과 동일 내용의 JSON |
| 사이 | 23,516–23,519 | 296 | 0.0% | — |
| 앱 JS | 23,520–24,515 | 45,363 | 6.1% | 렌더링 로직 전체 |
| 닫기 | 24,516–24,517 | 15 | 0.0% | — |

구조 랜드마크 (`grep -n` 실측):

```
   3:<head>
   8:  <style>
  10:  </style>
  11:  <script>            ← Chart.js
  27:  </script>
  28:</head>
  29:<body class="bg-slate-50 text-slate-900 min-h-screen p-4">
  34:<header ...>
  60:<section ...>          ← KPI 2칸 그리드
  83:<section ...>          ← 연도/기간 컨트롤 + 타임슬라이더
 124:<section ...>          ← 카토그램 + Top5
 149:<section ...>          ← 차트 탭 + districtChart
 159:<section ...>          ← commentary
 187:<script type="application/json" id="real-data">{
23515:}</script>
23520:<script>              ← 앱 JS
24515:</script>
24516:</body>
```

### 4.2 데이터 주입 방식

`[확인]` **별도 파일을 읽지 않는다. HTML 안에 직접 박혀 있다.**

- `fetch()` 호출: **0건** (`grep -c 'fetch(' dashboard.html` → 0)
- 외부 `src=` 스크립트: **0건**
- `dashboard.html` 내 `http://`/`https://` 문자열은 라이브러리 주석/저작권 URL 3개뿐 (`chartjs.org`, `tailwindcss.com`, `github.com/kurkle/color`)

주입 로직 — `~/Project/dashboard/ingest.mjs:476-499`:

```javascript
function inject(normalized) {
  const htmlPath = join(__dirname, 'dashboard.html');
  const jsonPath = join(__dirname, 'data.json');

  const html = readFileSync(htmlPath, 'utf8');
  const jsonStr = JSON.stringify(normalized, null, 2);

  const slotRe = /(<script\s+type="application\/json"\s+id="real-data">)([\s\S]*?)(<\/script>)/;

  // 슬롯 부재 = 진짜 오류. (내용 유무와 무관하게 슬롯 존재 여부로 판정 — 재실행 멱등)
  if (!slotRe.test(html)) {
    throw new Error('#real-data 슬롯을 찾지 못했습니다. dashboard.html을 확인하세요.');
  }

  const newHtml = html.replace(slotRe, `$1${jsonStr}$3`);
  writeFileSync(jsonPath, jsonStr, 'utf8');

  // 데이터 무변경(동일 스냅샷) = 정상 no-op. HTML 갱신 생략.
  if (newHtml === html) {
    console.log('[ingest] 데이터 변경 없음 — dashboard.html 갱신 생략 (data.json만 저장)');
    return;
  }

  writeFileSync(htmlPath, newHtml, 'utf8');
```

클라이언트 측 읽기 — `dashboard.html:23547-23548`:

```javascript
const RAW = JSON.parse(document.getElementById('real-data').textContent || 'null');
const IS_REAL = RAW !== null && typeof RAW === 'object' && RAW.byDistrict != null;
```

### 4.3 용량 증가 추정

현재 `#real-data`는 25구 × (54월 + 12주) = **1,650 period-cell**, 셀당 평균 **284 bytes** (`JSON.stringify(indent=2)` 기준).

| 시나리오 | 추가 내용 | 셀당 추가 | 총 증가 | 예상 `#real-data` | 예상 `dashboard.html` |
|---|---|---:|---:|---:|---:|
| 현행 | — | — | — | 469,767 | 748,573 |
| A. 셀당 통계 6개 추가 (`avgAmt`, `medAmt`, `p25`, `p75`, `avgArea`, `sumAmt`) | 숫자 6개 + 중첩 키 | **117 B** | **+193,050 B** | 662,817 | **≈ 941,623 B** |
| B. 거래 원본 443,363건 전량 인라인 | 캐시 원본 그대로 | — | +27,396,306 B | 27,866,073 | **≈ 28.1 MB** |

`[확인]` 시나리오 A는 실측 기반 계산. 시나리오 B의 27,396,306 B는 캐시 파일 실측 총합(공백 없는 1줄 JSON 기준)이며, indent 적용 시 더 커진다.

### 4.4 세 가지 모드의 분기

**모드 축이 3개다.**

**축 1 — 주간 / 월간** (`granularity`). `dashboard.html:23751`:

```javascript
let granularity      = 'week';
```

`dashboard.html:24476-24497`:

```javascript
function setGranularity(gran) {
  granularity = gran;
  periods     = dataProvider.getPeriods(gran);
  // viewRange를 전체 범위로 리셋 (스펙 요구사항)
  viewRange   = { start: 0, end: periods.length - 1 };
  periodIndex = viewRange.end;

  const wBtn = document.getElementById('btnWeek');
  const mBtn = document.getElementById('btnMonth');
  if (gran === 'week') {
    wBtn.className = 'px-3 py-1.5 rounded-md text-sm font-semibold transition-colors bg-indigo-600 text-white';
    mBtn.className = 'px-3 py-1.5 rounded-md text-sm font-semibold transition-colors text-slate-600 hover:text-slate-900';
  } else {
    mBtn.className = 'px-3 py-1.5 rounded-md text-sm font-semibold transition-colors bg-indigo-600 text-white';
    wBtn.className = 'px-3 py-1.5 rounded-md text-sm font-semibold transition-colors text-slate-600 hover:text-slate-900';
  }
  stopAutoplay();
  // 연도 버튼/드롭다운 DOM 재빌드 (granularity 변경 시마다)
  _buildYearControls();
  renderAll();
}
```

UI 진입점 — `dashboard.html:42-45` (`onclick="setGranularity('week')"` / `'month'`).

**축 2 — 실데이터 / 목데이터** (`IS_REAL`). `dashboard.html:23550-23555`:

```javascript
// 배지 + 헤더 수집일 업데이트 (수집일은 헤더로 일원화, 배지는 간결하게)
if (IS_REAL) {
  document.getElementById('dataBadge').textContent = '실데이터';
  document.getElementById('headerDate').textContent = `데이터 수집일: ${RAW.generatedAt}`;
}
```

목데이터 생성기는 `dataProvider` IIFE 내부(`dashboard.html:23561-23744`). 관련 함수: `_monthMultiplier()` (23568), `_weekVariation()` (23577), `_buildMonthPeriods()` (23583), `_buildWeekPeriods()` (23599), `_districtMonthlySeq()` (23621), `_districtWeeklySeq()` (23634), `_buildMockNormalized()` (23650).

기준 시각은 하드코딩 — `dashboard.html:23524`:

```javascript
const TODAY = new Date('2026-07-06');
```

폴백 분기는 셀 단위에도 존재 — `dashboard.html:23724-23740`:

```javascript
      // 실데이터: room/price 필드 직접 읽기
      if (entry.room && entry.price) {
        return { room: entry.room, price: entry.price };
      }

      // 목데이터 폴백: nonApt 기반 균등 분할 추정
      const nonApt = entry.nonApt;
      const total  = entry.apt + nonApt;
      const base3  = Math.floor(total / 3);
      return {
        room:  { studio: base3, two: base3, three: total - base3 * 2 },
        price: {
          under3: Math.floor(nonApt * 0.45),
          under6: Math.floor(nonApt * 0.40),
          over6:  nonApt - Math.floor(nonApt * 0.45) - Math.floor(nonApt * 0.40)
        }
      };
```

**축 3 — 차트 탭 trend / room / price** (`chartMode`). `dashboard.html:23761`:

```javascript
let chartMode         = 'trend'; // 'trend' | 'room' | 'price'
```

전환 함수 `switchChartMode(mode)` (`dashboard.html:24414`), UI는 `dashboard.html:163-167`의 `chartTab-trend` / `chartTab-room` / `chartTab-price` 버튼. `dashboard.html:24012`가 `chartMode !== 'room'` 일 때 `#chartNote` 를 숨긴다.

### 4.5 CSS 관리

`[확인]` **전량 인라인.** 별도 `.css` 파일 없음.

- `dashboard.html:8-10` — Tailwind 정적 빌드 산출물 11,992 bytes. `<style>` 태그 안 1줄.
- 컴포넌트 스타일은 마크업의 Tailwind 유틸리티 클래스로 표현.
- **색상 토큰이 한 곳에 모여 있지 않다.** `--color-*` 커스텀 프로퍼티나 `COLOR`/`PALETTE` 상수 정의는 **존재하지 않음** (`grep -n "COLOR\|PALETTE\|const.*color"` → 0건).
- JS 구간(23,520–24,515)에 하드코딩된 색상 리터럴 실측:

```
   7  rgba(
   4  #475569
   3  #e2e8f0
   2  rgb(
   2  #ffffff
   2  #cbd5e1
   2  #64748b
   2  #334155
   2  #0f172a
   1  #fbbf24
   1  #f1f5f9
   1  #e11d48
   1  #a855f7
   1  #6366f1
   1  #38bdf8
   1  #22c55e
```

- 추가로 Tailwind 클래스명 형태의 색상(`bg-indigo-600`, `text-slate-600`, `bg-indigo-200`, `bg-slate-100` 등)이 마크업과 JS의 `className` 문자열 양쪽에 흩어져 있다. 예: `setGranularity` (24485–24492행)가 버튼 색상 클래스를 문자열로 직접 재할당.

### 4.6 라이브러리

| 용도 | 구현 | 위치 |
|---|---|---|
| 차트 | **Chart.js v4.5.1** (인라인 번들 208,545 bytes) | `dashboard.html:11-27` |
| 스타일 | **Tailwind CSS** (정적 빌드 인라인 11,992 bytes) | `dashboard.html:8-10` |
| 지도 | **없음 — 자체 CSS Grid 카토그램** | `dashboard.html:136`, `cartogramLayout` (`dashboard.html:23768`), `renderCartogram()` (`dashboard.html:23891`) |
| 표 | **없음** — `<table>` 태그 0개. Top5는 `<ol id="top5List">` (`dashboard.html:141`) + `renderTop5()` (`dashboard.html:23953`) |

Chart.js 배너 원문 — `dashboard.html:12-17`:

```
/*!
 * Chart.js v4.5.1
 * https://www.chartjs.org
 * (c) 2025 Chart.js Contributors
 * Released under the MIT License
 */
```

카토그램 마크업 — `dashboard.html:136`:

```html
      <div class="grid gap-1.5" id="cartogramMap" role="group" aria-label="서울시 25개구 카토그램" style="grid-template-columns: repeat(8, 1fr);"></div>
```

`[확인]` Leaflet / Mapbox / D3 / GeoJSON 미사용. 실제 지리 좌표가 아니라 8열 그리드에 25개 구를 배치한 도식.

### 4.7 주요 렌더 함수 목록 (`dashboard.html`)

```
23782 hasBatchim(word)              23953 renderTop5()
23788 josa(word, pair)              23995 CHART_LIGHT_OPTS
23796 formatPeriodLabel(key, gran)  24008 renderDistrictChart()
23805 deltaPct(curr, prev)          24070 renderCommentary()
23810 renderDelta(elementId, ...)   24127 renderNavigation()
23826 periodShortLabel(key, gran)   24146 renderBarChart()
23839 renderKPI()                   24223 _buildYearControls()
23891 renderCartogram()             24304 _yearBtnClass(active)
                                    24311 _syncYearControls()
                                    24350 _yearRange(year)
                                    24362 _setYearFilter(year)
                                    24381 _onRangeStartChange(e)
                                    24391 _onRangeEndChange(e)
                                    24401 renderAll()
                                    24414 switchChartMode(mode)
                                    24434 stopAutoplay()
                                    24444 toggleAutoplay()
                                    24476 setGranularity(gran)
                                    24498 navigate(dir)
                                    24506 selectDistrict(district)
```

상태 변수는 `dashboard.html:23751-23765`에 단일 블록으로 모여 있고, `viewRange`에 불변식 주석이 달려 있다 — `dashboard.html:23755-23757`:

```javascript
// @MX:ANCHOR: [AUTO] viewRange is the single source of truth for slider/nav/autoplay range
// @MX:REASON: 4-control desync bug prevention — all controls read from this, never write to each other
let viewRange        = { start: 0, end: 0 };
```

---

## 5. 배포와 자동 실행

### 5.1 실행 순서

```
launchctl (com.kuka.dashboard-ingest, 매주 월 07:00)
  └─ ~/Project/dashboard/scripts/run-ingest.sh
       ├─ mv ingest-launchd.log → ingest-launchd.log.prev   (run-ingest.sh:19)
       ├─ cd ~/Project                            (run-ingest.sh:26)
       ├─ /usr/local/bin/node dashboard/ingest.mjs          (run-ingest.sh:27)
       │    ├─ .env 로드                                    (ingest.mjs:24-33)
       │    ├─ buildPeriods(runDate) → dealYmdList / monthPeriods / weekPeriods
       │    ├─ 25구 × 55개월 × 4유형 루프                    (ingest.mjs:1327-1346)
       │    │    └─ fetchCombo() → 캐시 히트 or fetchOnePage() ×N (200ms 간격)
       │    │         최근 2개월은 캐시 무시 재수집           (ingest.mjs:1330-1331)
       │    ├─ printCrossCheck()                            (ingest.mjs:1364)
       │    ├─ buildNormalized()                            (ingest.mjs:1395)
       │    ├─ inject() → dashboard.html + data.json 쓰기    (ingest.mjs:1398)
       │    └─ 실패 콤보 있으면 process.exit(1)              (ingest.mjs:1402-1404)
       └─ rc == 0 이면 sh deploy.sh                          (run-ingest.sh:37-45)
            ├─ generatedAt 추출                              (deploy.sh:34-36)
            ├─ gh auth setup-git                             (deploy.sh:43)
            ├─ ~/.cache/seoul-property-dashboard fetch/reset (deploy.sh:46-52)
            ├─ cmp -s 로 동일하면 exit 0 (no-op)             (deploy.sh:57-60)
            ├─ cp dashboard.html → index.html, commit, push  (deploy.sh:63-70)
            ├─ Pages 빌드 폴링 (10초 × 24회 = 최대 240초)     (deploy.sh:75-82)
            └─ 라이브 sha256 검증 (5초 × 18회 = 최대 90초)    (deploy.sh:86-96)
```

`run-ingest.sh:19` 로그 로테이션:

```sh
# 직전 실행 로그를 .prev 로 이동(1세대 보관). 항상 현재+직전 2개만 유지.
[ -f "$LOG" ] && mv -f "$LOG" "$LOG.prev"
```

`run-ingest.sh:35-45` 배포 게이트:

```sh
  # 배포: ingest 가 exit 0 으로 성공한 경우에만 호출한다.
  # 실패(rc!=0)면 스킵하고 사유를 로그에 남긴다 — 실패한 데이터가 라이브에 올라가지 않게.
  if [ "$rc" -eq 0 ]; then
    echo "=== deploy 시작: $(date '+%Y-%m-%d %H:%M:%S %Z') ==="
    sh "$SCRIPT_DIR/deploy.sh"
    drc=$?
    echo "=== deploy 종료 (exit=$drc): $(date '+%Y-%m-%d %H:%M:%S %Z') ==="
    rc="$drc"
  else
    echo "[deploy] ingest 실패(rc=$rc) → 배포 스킵 (실패 데이터 라이브 미반영)"
  fi
```

배포 대상 — `deploy.sh:13-15`:

```sh
OWNER="byoneself4023-bit"
REPO="seoul-property-dashboard"
LIVE_URL="https://byoneself4023-bit.github.io/seoul-property-dashboard/"
```

배포 커밋 서명 — `deploy.sh:65-69`:

```sh
git -C "$CACHE_DIR" \
  -c user.name="쿠카" -c user.email="223078465+byoneself4023-bit@users.noreply.github.com" \
  commit -q -m "chore: 대시보드 갱신 — 수집일 ${DATA_DATE} (원본 ${SRC_HASH})

🗿 MoAI <email@mo.ai.kr>" || fail "commit 실패"
```

### 5.2 스케줄 설정 원문

`~/Library/LaunchAgents/com.kuka.dashboard-ingest.plist` 전문:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.kuka.dashboard-ingest</string>
  <key>ProgramArguments</key>
  <array>
    <string>~/Project/dashboard/scripts/run-ingest.sh</string>
  </array>
  <key>EnvironmentVariables</key>
  <dict>
    <key>INGEST_NODE</key>
    <string>/usr/local/bin/node</string>
  </dict>
  <key>WorkingDirectory</key>
  <string>~/Project</string>
  <key>StartCalendarInterval</key>
  <dict>
    <key>Weekday</key>
    <integer>1</integer>
    <key>Hour</key>
    <integer>7</integer>
    <key>Minute</key>
    <integer>0</integer>
  </dict>
  <key>RunAtLoad</key>
  <false/>
  <key>ProcessType</key>
  <string>Background</string>
  <key>StandardOutPath</key>
  <string>/dev/null</string>
  <key>StandardErrorPath</key>
  <string>~/Project/dashboard/logs/launchd-stderr.log</string>
</dict>
</plist>
```

주기: **매주 월요일(`Weekday` 1) 07:00 KST**, 1주 1회. `RunAtLoad=false`.

이 plist를 생성하는 소스 — `dashboard/scripts/install-scheduler.sh:19-22`:

```sh
# 스케줄 (launchd Weekday: 0/7=일, 1=월 ... 6=토)
START_WEEKDAY=1
START_HOUR=7
START_MINUTE=0
```

`[확인]` cron 은 사용하지 않는다. 스케줄링은 전부 launchd.

### 5.3 마지막 성공 실행

`~/Project/dashboard/logs/ingest-launchd.log` 원문 (1–16행):

```
=== ingest 시작: 2026-07-13 15:36:57 KST ===
PROJECT=~/Project
NODE=/usr/local/bin/node
[ingest] 캐시 모드: 이미 수집된 콤보는 건너뜁니다.
[ingest] (재수집하려면 --fresh 플래그를 사용하세요.)

[ingest] 실행 날짜: 2026-07-13
[ingest] 수집 기간: 202201 ~ 202607
[ingest] 유형: apt, rh, sh, offi (4종)
[ingest] 예상 요청 수(캐시 미스 시): 25구 × 55개월 × 4종 = 5500건
[ingest] 일일 한도: 10,000건 — 여유 있음

[ingest] ⚠️  오피스텔 API는 별도 활용신청 필요 — 미신청 시 해당 유형만 건너뜁니다
[ingest] 최근 2개월(202606, 202607)은 신고 지연 반영을 위해 캐시 무시하고 재수집

[ingest] 종로구 (11110) 수집 중...
```

같은 로그 67–89행:

```
[ingest] 교차검증 중...
[교차검증] 전체 콤보 totalCount === 수집건수 일치 ✓

[ingest] 수집 완료 요약
  총 API 호출: 200건 (캐시 히트: 5300건)
    apt  : 호출 50건 · 캐시 1325건 · 실패 0건
    rh   : 호출 50건 · 캐시 1325건 · 실패 0건
    sh   : 호출 50건 · 캐시 1325건 · 실패 0건
    offi : 호출 50건 · 캐시 1325건 · 실패 0건
    합계  : 호출 200건 · 캐시 5300건 · 실패 0건
  실패 콤보:   0건

[ingest] 집계 중...
[ingest] dashboard.html 및 data.json 저장 중...
[ingest] dashboard.html 주입 완료 (generatedAt: 2026-07-13)
[ingest] data.json 저장 완료
[ingest] 완료!
=== ingest 종료 (exit=0): 2026-07-13 15:43:11 KST ===
=== deploy 시작: 2026-07-13 15:43:11 KST ===
[deploy] 푸시 완료 — 수집일 2026-07-13, 원본 a310c11-dirty
[deploy] Pages 빌드 대기...
[deploy] 배포 완료 ✅ https://byoneself4023-bit.github.io/seoul-property-dashboard/ — 수집일 2026-07-13 라이브 반영 확인 (원본 a310c11-dirty)
=== deploy 종료 (exit=0): 2026-07-13 15:43:46 KST ===
```

**`[확인]` 마지막 성공 실행 = 2026-07-13 15:43:46 KST. 조사일(2026-08-12) 기준 30일 전.**

배포 저장소 캐시(`~/.cache/seoul-property-dashboard`) 상태:

```
-rw-r--r--@  1 kuka  staff  748573 Jul 13 15:43 index.html

5644a87 2026-07-13 chore: 대시보드 갱신 — 수집일 2026-07-13 (원본 a310c11-dirty)
ce921a8 2026-07-13 chore: 대시보드 갱신 — 수집일 2026-07-12 (원본 b802893)
d544387 2026-07-13 test: stale marker (deploy 경로 검증용)
```

`[확인]` 라이브 사이트도 2026-07-13 스냅샷에서 멈춰 있다.

`launchctl print gui/501/com.kuka.dashboard-ingest` 발췌:

```
	active count = 0
	state = not running
	runs = 0
	last exit code = (never exited)
```

`[확인]` 07-13 이후 예정된 월요일 07:00 은 07-20, 07-27, 08-03, 08-10 — **4회. 실행 흔적 없음.** `run-ingest.sh:19`가 실행 시마다 로그를 `.prev`로 밀어내므로, 실행되었다면 현재 로그가 최신 날짜여야 한다. 현재 로그 07-13 / `.prev` 07-12 → **미실행 확정**.

`[미확인]` 미실행 원인. 유력한 가설은 `RunAtLoad=false` + 월요일 07:00 에 맥이 완전 종료(shutdown) 상태. 문서(`~/Project/dashboard/docs/실데이터_연동_가이드.md:89`)가 이 한계를 명시하고 있다:

```
- **완전 종료(shutdown) 상태**로 07:00 을 지나침 → launchd 는 이 경우를 **소급 실행하지 않는다**. 다음 월요일 07:00 까지 대기한다. ❌ (launchd 한계 — 보장 안 됨)
```

다만 4주 연속 전원이 꺼져 있었는지는 이번 조사로 검증하지 못했다. `launchctl print`의 `runs = 0`은 **현재 로그인 세션 기준** 카운터라 단독으로는 근거가 되지 않는다.

### 5.4 실패 시 동작

**1) 일일 한도 초과** — `ingest.mjs:1353-1360`:

```javascript
  } catch (err) {
    if (err.isQuotaExceeded) {
      console.error(`\n[ingest] ${err.message}`);
      console.error('[ingest] 내일 node ingest.mjs 를 재실행하면 캐시로 이어서 진행됩니다.');
      process.exit(1);
    }
    throw err;
  }
```

→ `inject()` 도달 전 종료. `dashboard.html` 미변경.

**2) 개별 콤보 실패 (재시도 3회 소진)** — `ingest.mjs:620-627`:

```javascript
  // MAX_RETRY 모두 실패 → 목록에 기록하고 null 반환 (중단 없이 계속)
  stats.failures.push({
    combo: `${lawdCd}/${dealYmd}/${propType}`,
    reason: lastErr.message
  });
  console.error(`  [실패] ${lawdCd}/${dealYmd}/${propType}: ${lastErr.message}`);
  return null;
```

→ 루프는 계속되고, `ingest.mjs:1398` `inject(normalized);` 가 **먼저 실행되어 `dashboard.html` 이 부분 데이터로 덮어써진다.** 그 뒤 `ingest.mjs:1401-1404`:

```javascript
  // 실패가 있으면 exit code 1
  if (stats.failures.length > 0) {
    process.exit(1);
  }
```

→ `run-ingest.sh:43-45` 가 rc≠0을 보고 배포를 건너뛴다.

`[확인]` 즉 **부분 실패 시 로컬 `dashboard.html`은 열화된 데이터로 갱신되지만 라이브에는 반영되지 않는다.**

**3) 배포 단계 실패** — `deploy.sh:26`:

```sh
fail() { echo "[deploy] 실패: $*" >&2; exit 1; }
```

`gh` 미설치·인증 실패·push 실패·Pages 빌드 timeout(240초)·라이브 sha256 불일치(90초) 각각에서 exit 1. `run-ingest.sh:42` 가 `rc="$drc"` 로 이를 전체 종료코드로 승격.

**4) 알림 없음** — `[확인]` 실패 통지 경로가 존재하지 않는다. plist의 `StandardOutPath`는 `/dev/null`, `StandardErrorPath`는 `launchd-stderr.log`(현재 0 bytes). 사람이 `~/Project/dashboard/logs/ingest-launchd.log` 를 직접 열지 않으면 실패도, 미실행도 알 수 없다.

---

## 6. 문서 현황

### 6.1 md 문서 전체 (`.claude/skills/**` 제외, 51개)

**루트·제품 문서**

| 경로 | bytes | 내용 |
|---|---:|---|
| `~/Project/CLAUDE.md` | 28,077 | MoAI 실행 지시서 (§6.2) |
| `~/Project/CHANGELOG.md` | 1,038 | 과제 1 변경 이력 1건 + Unreleased |
| `~/Project/docs/전체플랫폼_스택제안.md` | 5,639 | 플랫폼 전체 기술 스택 제안 (배경 / 제품별 요건 / 추천 스택 / 트레이드오프 / 실데이터 프록시 필요성 / 단일 HTML 선택 근거) |
| `~/Project/dashboard/docs/실데이터_연동_가이드.md` | 13,445 | **유일한 제품 운영 문서.** API 키 발급 / 로컬 설정 / 실행 / 신고 지연 / launchd 등록·해제 / 모노레포 작업 규칙 / API 스펙 부록 |

**`.moai/` 문서 (21개)**

| 경로 | bytes | 내용 |
|---|---:|---|
| `.moai/plans/cozy-gliding-lightning.md` | 2,818 | 커밋 ⑩ 실데이터 적재 실행 플랜. 후속으로 뉴스레터 언급 |
| `.moai/design/README.md` / `research.md` / `system.md` / `spec.md` | 1,835 / 1,131 / 1,462 / 2,063 | 디자인 워크플로 문서 |
| `.moai/project/brand/brand-voice.md` / `target-audience.md` / `visual-identity.md` | 1,713 / 2,227 / 2,646 | 브랜드 컨텍스트 |
| `.moai/project/db/README.md` / `schema.md` / `migrations.md` / `queries.md` / `rls-policies.md` / `seed-data.md` | 3,293 / 1,894 / 1,692 / 2,211 / 1,966 / 2,290 | DB 메타 문서 |
| `.moai/config/evaluator-profiles/{default,frontend,lenient,strict}.md` | 2,342 / 3,798 / 2,166 / 2,776 | 평가 프로파일 |
| `.moai/docs/MCP_OAUTH_SETUP.md` | 4,261 | MCP OAuth 설정 |
| `.moai/evolution/changelog.md` | 182 | 진화 로그 |
| `.moai/learning/README.md` | 217 | — |
| `.moai/state/session-memo.md` | 133 | — |
| `.moai/reports/session-*.md` (10개) | 429~663 각 | 세션 리포트 (gitignored, `.gitignore:190`) |

**`drive-docs/md/` (19개)** — 외부 문서 변환본. 사업 문서. 대시보드 코드와 직접 연결 없음. 최대 파일 39,907 B.

### 6.2 `CLAUDE.md` 요약

`~/Project/CLAUDE.md` — 28,077 bytes, 17개 섹션. 목차 실측:

```
  1:# MoAI Execution Directive
  3:## 1. Core Identity
 30:## 2. Request Processing Pipeline
 73:## 3. Command Reference
 95:## 4. Agent Catalog
139:## 5. SPEC-Based Workflow
180:## 6. Quality Gates
209:## 7. Safe Development Protocol
297:## 8. User Interaction Architecture
385:## 9. Configuration Reference
422:## 10. Web Search Protocol
440:## 11. Error Handling
458:## 12. MCP Servers & Deep Analysis Modes
473:## 13. Progressive Disclosure System
489:## 14. Parallel Execution Safeguards
511:## 15. Agent Teams (Experimental)
579:## 16. Context Search Protocol
626:## 17. Troubleshooting
```

`[확인]` 내용은 **전부 MoAI-ADK 프레임워크 운영 규칙**이다 — 에이전트 위임 원칙, SPEC 기반 Plan-Run-Sync 워크플로, TRUST 5 품질 게이트, AskUserQuestion 전용 상호작용 규칙, 병렬 실행 안전장치, Agent Teams(실험적), CG Mode, MCP 서버 목록, 트러블슈팅.

**`[확인]` 이 저장소가 무슨 제품을 담고 있는지에 대한 서술이 `CLAUDE.md` 에 전혀 없다.** `dashboard`, `RTMS`, `서울`, `부동산`, `국토교통부` 어느 문자열도 등장하지 않는다. 제품 맥락은 `dashboard/docs/실데이터_연동_가이드.md` 에만 존재한다.

### 6.3 코드-문서 불일치

**불일치 1 — 차트 라이브러리** `[확인]`

`~/Project/CHANGELOG.md:18`:

```
- Single-file dashboard component with Echarts visualization
```

실제: Chart.js v4.5.1 (`dashboard.html:12-17` 저작권 배너). ECharts는 저장소 어디에도 없음.

**불일치 2 — 전월세 데이터** `[확인]`

`~/Project/CHANGELOG.md:16`:

```
- Per-type transaction stats (apt sale, apt lease, non-apt sale, non-apt lease, officetel sale)
```

실제: `ingest.mjs:88-93` 의 엔드포인트 4종은 전부 매매(`Trade`). `apt lease` / `non-apt lease` 에 해당하는 전월세 API(`RTMSDataSvcAptRent` 계열)는 호출하지 않는다.

**불일치 3 — 건수 산출 근거** `[확인]`

`~/Project/dashboard/docs/실데이터_연동_가이드.md:127`:

```
  - **건수 = 응답의 `<totalCount>` 필드** (개별 `<item>` 개수가 아님). `totalCount > numOfRows` 이면 `pageNo` 를 증가시켜 전체 페이지를 수집한다.
```

실제: `ingest.mjs:353-357` 은 item 을 하나씩 순회하며 카운트한다.

```javascript
    // 월 인덱스
    const mIdx = monthPeriods.indexOf(ymKey);
    if (mIdx >= 0) {
      // 건수 (변경 없음)
      monthMap[ymKey]++;
```

`totalCount` 는 페이지네이션 종료 조건(`ingest.mjs:582`)과 교차검증(`ingest.mjs:592-603`, `printCrossCheck` `ingest.mjs:640-654`)에만 쓰인다. 마지막 실행에서 교차검증이 전건 일치했으므로 **결과 수치는 같지만, 문서가 서술한 산출 경로는 코드와 다르다.**

**불일치 4 — 실패 시 주입 시점** `[확인]`

`~/Project/dashboard/docs/실데이터_연동_가이드.md:94`:

```
`ingest.mjs` 의 dashboard.html 주입(`inject`)은 **수집·집계가 성공한 뒤에만** 일어난다. 네트워크 장애·API 한도 등으로 수집이 중단되면 주입 전에 종료(exit 1)되므로 **기존 dashboard.html 은 직전 스냅샷 그대로 유효**하다. 부분 실패(일부 콤보만 실패)여도 캐시 기반 최선값으로 정상 스냅샷을 유지한다.
```

전반부(한도 초과·throw)는 정확하다(`ingest.mjs:1353-1360`). 후반부는 부정확하다 — 부분 실패 시 `inject()`(`ingest.mjs:1398`)가 `process.exit(1)`(`ingest.mjs:1401-1404`)보다 **먼저** 실행되므로 `dashboard.html` 은 실제로 덮어써진다. "직전 스냅샷 그대로 유효"한 것은 **라이브 사이트**이지 로컬 파일이 아니다.

**불일치 5 — 금액대 차트의 아파트 제외** `[확인, 문서는 정확]`

`실데이터_연동_가이드.md:128` 은 "아파트는 금액대 차트에서 제외(라벨이 '금액대별(비아파트)')" 라고 정확히 기술하고 있다. 코드(`ingest.mjs:439-443`)와 일치. 다만 이 제약이 §7.1의 시장 리포트 요구와 충돌하므로 여기 기록한다.

**불일치 6 — 문서 내 제품 목록** `[확인]`

커밋 `9832639` (2026-07-20) 가 `실데이터_연동_가이드.md` 에서 newsletter 참조 2줄을 일반화했다(`1 file changed, 2 insertions(+), 2 deletions(-)`). 현재 `실데이터_연동_가이드.md:103`:

```
이 저장소는 제품을 **폴더**로 나눈 단일 저장소(모노레포)다: 현재는 `dashboard/` 하나이며, 이후 추가되는 제품도 최상위 하위 폴더로 둔다. `.moai/` 는 루트 1개가 전체를 관리한다.
```

이 문장과 달리 워킹트리에는 `newsletter/` 폴더가 실재한다(§2.1). 문서가 맞고 파일시스템이 틀린 케이스.

`[미확인]` `.moai/project/db/*.md` (`schema` / `migrations` / `queries` / `rls-policies` / `seed-data`) 가 어떤 실제 DB를 서술하는지. 이 저장소에는 DB 마이그레이션 파일도 DB 접속 코드도 없다. 템플릿 초기 상태인지 다른 제품용 기록인지 판별하지 못했다.

---

## 7. 종합

### 7.1 시장 리포트 화면을 얹을 때 걸림돌

**걸림돌 1 — 집계 계층에 금액 원값이 없다** (최대 제약)

원본 캐시(`.cache/ingest/`)에는 443,363건의 `amount` 가 모두 살아 있다(§3.5). 그러나 `data.json` / `#real-data` 에는 3구간 버킷 카운트만 남는다(§3.6). 평균가·중앙값·㎡당 단가·총거래액·전기 대비 변동률 중 **어느 것도 현재 산출물로는 계산 불가.**

- 수정 지점: `ingest.mjs:342-348` (`priceBucket`), `ingest.mjs:311-399` (`aggregateItems`), `ingest.mjs:421-461` (`buildNormalized`)
- **재수집 불필요** — 캐시가 원본을 갖고 있으므로 `node dashboard/ingest.mjs` 1회 실행이면 전 기간 재집계된다 (`fetchCombo` 가 schemaVersion 2 캐시를 히트, `ingest.mjs:556-563`)
- 비용: `#real-data` +193,050 bytes, `dashboard.html` ≈ 941,623 bytes (§4.3 시나리오 A)

**걸림돌 2 — 금액 집계에서 아파트가 빠져 있다**

`ingest.mjs:439-443` 의 `price` 는 `rhAgg + shAgg + offiAgg` 만 합산한다. 아파트 229,980건(전체 51.9%)이 금액 축에서 통째로 제외된다. 시장 리포트는 통상 아파트가 주력이므로 **집계 정의 자체를 바꿔야 한다.** 기존 "금액대별(비아파트)" 차트(`dashboard.html:167`, `renderDistrictChart` `dashboard.html:24008`)의 의미가 달라지므로 유지/대체 결정이 선행되어야 한다.

**걸림돌 3 — 단지·동·층 식별자가 없다**

`parseItems` (`ingest.mjs:223-260`) 가 `aptNm` / `umdNm` / `floor` / `buildYear` 를 버린다(§3.3). 따라서 다음은 **현재 캐시로는 불가능하며 5,500 콤보 전량 재수집이 필요**하다:

- 단지별 실거래 리스트 / 최고가 단지 랭킹
- 법정동 단위 세분화 (현재 최소 단위는 구)
- 층·연식 보정 시세
- 동일 단지 반복거래 기반 가격지수

재수집 비용: 5,500 요청 × `DELAY_MS` 200ms = 최소 18.3분. 일일 한도 10,000건 내(`ingest.mjs:39` 주석). `parseItems` 확장 후 `cacheSave`(`ingest.mjs:288`)의 `schemaVersion` 을 3으로 올리면 `ingest.mjs:558` 의 `cached.schemaVersion === 2` 조건이 거짓이 되어 **전량 자동 재수집**된다 — 무효화 메커니즘은 이미 갖춰져 있다.

**걸림돌 4 — sh(단독/다가구) 면적 100% null**

17,029건. API가 `excluUseAr` 을 주지 않는다(§3.2 sh 응답). 면적 기반 지표(㎡당 단가 등)에서 이 유형은 구조적으로 제외되거나, `plottageAr` / `totalFloorAr` 을 신규 필드로 추가 수집해야 한다.

**걸림돌 5 — 단일 HTML 인라인 모델의 용량 천장**

`dashboard.html` 748,573 bytes 중 62.8%가 이미 데이터다(§4.1). 거래 원본을 화면에 넣으면 28.1MB(§4.3 시나리오 B) — GitHub Pages 정적 서빙과 브라우저 파싱 양쪽에서 비현실적. **집계 통계만 인라인하거나, `fetch()` 기반 외부 데이터 파일로 모델을 전환하는 결정이 필요하다.** 현재 `fetch()` 0건·외부 리소스 0건이며, `verify-quality.mjs:25` 의 `externalScripts` 검사가 이 규율을 강제하고 있으므로 품질 게이트도 함께 수정 대상이 된다.

**걸림돌 6 — 색상 토큰 미중앙화**

`--color-*` 정의도 `PALETTE` 상수도 없다(§4.5). 색상이 Tailwind 클래스 문자열과 15종 이상의 hex 리터럴로 마크업·JS에 흩어져 있다. 리포트 화면을 추가하며 시각적 일관성을 맞추려면 토큰 정리가 선행 과제로 붙는다.

**걸림돌 7 — 데이터가 30일 낡았다**

`generatedAt: "2026-07-13"`, 라이브도 동일(§5.3). 리포트를 얹기 전에 수집 자동화가 먼저 정상화되어야 한다.

**걸림돌 8 — 목데이터 폴백 경로**

`dashboard.html:23724-23740` 의 폴백은 `entry.room && entry.price` 유무로 분기한다. 새 통계 필드를 추가하면 목데이터 생성기(`_buildMockNormalized`, `dashboard.html:23650`)에도 대응 필드를 넣어야 폴백이 깨지지 않는다.

### 7.2 뉴스레터 잔재 처리 판단

**지워도 되는 것** `[확인]`

| 대상 | 회수 용량 |
|---|---:|
| `~/Project/newsletter/logs/ui-server.log` | 58,484,440 B |
| `~/Project/newsletter/logs/scheduler.log` | 40,716 B |
| `~/Project/newsletter/` 디렉터리 | — |
| **합계** | **≈ 58.5 MB** |

근거: git 미추적(`git ls-files newsletter` → 0) + `.gitignore:107` 무시 + 실행 코드 참조 0건 + 내용이 MODULE_NOT_FOUND 스택트레이스 78,904회 반복. 코드 이력은 커밋 `1d3d7c3` 에 보존되어 있어 복구 가능.

**단, 선행 조건이 있다** — LaunchAgent를 먼저 해제하지 않으면 `KeepAlive=true` 인 `com.newsletter.ui` 가 로그 디렉터리를 즉시 재생성하고 루프를 계속한다. 필요한 순서:

1. `launchctl bootout gui/501/com.newsletter.ui`
2. `launchctl bootout gui/501/com.newsletter.daily`
3. `~/Library/LaunchAgents/com.newsletter.ui.plist` 삭제
4. `~/Library/LaunchAgents/com.newsletter.daily.plist` 삭제
5. `~/Project/newsletter/` 디렉터리 삭제

(이번 조사에서는 실행하지 않았다. 지시에 따라 판단 근거만 기록한다.)

**확인이 더 필요한 것**

| 대상 | 확인할 내용 |
|---|---|
| `.moai/plans/cozy-gliding-lightning.md:36` | "과제 2 뉴스레터(찐소장) 착수" 문장. 과제 2를 완전 폐기했다면 이력 주석 추가 여부 |
| `docs/전체플랫폼_스택제안.md:16,28,37` | 제품 목록에 뉴스레터가 살아 있음. 스택 제안 자체가 유효한 문서인지 |
| `~/Project/__pycache__/.scratch_reflow.cpython-312.pyc` | 대응 `.py` 없는 고아. 뉴스레터 관련인지 별건인지 미확인 |
| `.moai/project/db/*.md` 6개 | 실제 DB 없이 존재. 템플릿 잔재 가능성 |

**보존해야 하는 것**

| 대상 | 이유 |
|---|---|
| `drive-docs/md/데일리_부동산_뉴스레터_기획안.md` (15,885 B) | 외부 기획 문서 원본. 코드 잔재 아님 |
| `drive-docs/데일리_부동산_뉴스레터_기획안.pdf` (218,482 B) | 위 원본 PDF |
| 커밋 `1d3d7c3` 이전 git 이력 13개 커밋 | 되돌릴 유일한 경로 |
| `.claude/skills/**` 의 `newsletter` 문자열 9건 | 서드파티 스킬 예제. 이 제품과 무관 |

### 7.3 지금 구조에서 이상하거나 위험해 보이는 것

**A. 자동 수집이 4주째 죽어 있고, 아무도 모른다** `[확인]` — 심각

마지막 성공 2026-07-13, 예정 실행 4회 미발생(§5.3). 실패·미실행 통지 경로가 없다(§5.4-4). plist의 `StandardOutPath`가 `/dev/null` 이라 launchd 레벨 실패조차 흔적이 남지 않는다. 사람이 로그를 열어보기 전까지 라이브 대시보드가 낡아가는 것을 알 수 없는 구조.

**B. `KeepAlive` LaunchAgent가 58MB 로그를 계속 쓰고 있다** `[확인]` — 심각

`com.newsletter.ui` 가 10초마다 재기동 실패를 반복하며 `ui-server.log` 에 스택트레이스를 append 중. 조사 시점(12:59)에도 mtime 갱신 확인. 로테이션·상한 없음. 방치 시 디스크를 무한 잠식한다.

**C. 443,363건 원본 데이터가 이 맥에만 있다** `[확인]` — 높음

`.cache/ingest/` 27,396,306 B는 `.gitignore:230` 으로 미추적. 백업 경로 없음. 유실 시 5,500회 재수집(최소 18.3분 + API 한도 소모)이 필요하고, **2022~2024년 과거 데이터를 API가 여전히 제공하는지는 검증되지 않았다** `[미확인]`. 4년치 시계열이 단일 로컬 디렉터리에 걸려 있다.

**D. `.git` 이 pack 없이 loose object 1,260개, 50.7 MiB** `[확인]` — 중간

`git gc` 이력 없음(§1.3). 데이터 인라인 모델상 수집 1회 = 750KB blob 1개가 영구 누적된다. 주 1회 수집이 정상화되면 연간 약 39MB 증가. `git gc` 로 상당 부분 압축 가능하지만, 근본적으로는 데이터를 커밋 대상에서 분리해야 한다.

**E. 원격이 없는 git 저장소** `[확인]` — 높음

`git remote -v` 출력 없음. 코드·문서·git 이력 전체가 이 맥 로컬에만 존재한다. 배포용 `seoul-property-dashboard` 는 산출물 `index.html` 1개만 보관하므로 소스 백업이 아니다.

**F. `.env` 에 두 개의 실 크레덴셜** `[확인]` — 중간

`RTMS_SERVICE_KEY`, `ANTHROPIC_API_KEY`. `.gitignore:125-126` 로 보호되고 `git check-ignore` 확인됨. 다만 `deploy.sh:66` 이 커밋 author 로 실 이메일(`223078465+byoneself4023-bit@users.noreply.github.com`)을 **공개 저장소**에 기록한다. 의도된 것인지 확인 필요.

**G. RTMS API 4종을 평문 HTTP로 호출** `[확인]` — 중간

`ingest.mjs:89-92` 전부 `http://`. `serviceKey` 가 쿼리스트링에 평문으로 실려 나간다. data.go.kr 이 https 를 지원하는지는 `[미확인]`.

**H. 부분 실패가 로컬 파일을 조용히 열화시킨다** `[확인]` — 중간

§5.4-2. `inject()`(`ingest.mjs:1398`)가 `process.exit(1)`(`ingest.mjs:1403`)보다 먼저 실행된다. 배포는 막히지만 로컬 `dashboard.html` 은 이미 덮어써진 상태. 사람이 그 파일을 수동 배포하거나 커밋하면 열화 데이터가 반영된다. 문서(`실데이터_연동_가이드.md:94`)는 이와 다르게 서술하고 있다.

**I. 하드코딩된 기준 시각** `[확인]` — 낮음

`dashboard.html:23524` `const TODAY = new Date('2026-07-06');`. 목데이터 모드의 기간 계산에 쓰인다. 실데이터 모드에서는 `RAW.periods` 를 쓰므로 현재 영향은 없으나, 폴백이 발동하면 2026-07-06 기준 기간이 화면에 나타난다.

**J. `sumArr` 미사용 헬퍼** `[확인]` — 낮음

`ingest.mjs:427` 에 정의되어 있으나 호출 지점 없음. 리팩터링 잔재로 보인다.

**K. 제품 문서가 `CLAUDE.md` 에 없다** `[확인]` — 낮음

§6.2. 저장소가 무엇을 만드는지 아는 유일한 문서가 `dashboard/docs/실데이터_연동_가이드.md` 하나다. 새 참여자나 새 세션이 제품 맥락을 잡기 어렵다.

---

## 부록: 조사에 사용한 명령

```sh
# 구조·용량
ls -la ~/Project
du -sh ~/Project/*/ ~/Project/.[a-z]*
find ~/Project -name .git -maxdepth 4 -not -path "*/node_modules/*"

# git
git -C ~/Project branch -a
git -C ~/Project status --short
git -C ~/Project log -5 --format='%h %ad %s' --date=short
git -C ~/Project remote -v
git -C ~/Project count-objects -vH
git -C ~/Project show --stat 1d3d7c3
git -C ~/Project check-ignore -v newsletter/logs/ui-server.log
git -C ~/Project ls-files newsletter

# 뉴스레터
grep -rn -i "newsletter" ~/Project --exclude-dir={node_modules,.git,logs,.cache}
grep -c "MODULE_NOT_FOUND" ~/Project/newsletter/logs/ui-server.log
launchctl list | grep -iE "newsletter|kuka"
launchctl print gui/501/com.newsletter.ui
launchctl print gui/501/com.kuka.dashboard-ingest

# API 실응답 (읽기 전용 GET 4회, serviceKey 는 .env 에서 주입)
curl -s "http://apis.data.go.kr/1613000/RTMSDataSvcAptTradeDev/getRTMSDataSvcAptTradeDev?serviceKey=***&LAWD_CD=11110&DEAL_YMD=202605&pageNo=1&numOfRows=1"
curl -s "http://apis.data.go.kr/1613000/RTMSDataSvcRHTrade/getRTMSDataSvcRHTrade?serviceKey=***&LAWD_CD=11110&DEAL_YMD=202605&pageNo=1&numOfRows=1"
curl -s "http://apis.data.go.kr/1613000/RTMSDataSvcSHTrade/getRTMSDataSvcSHTrade?serviceKey=***&LAWD_CD=11110&DEAL_YMD=202605&pageNo=1&numOfRows=1"
curl -s "http://apis.data.go.kr/1613000/RTMSDataSvcOffiTrade/getRTMSDataSvcOffiTrade?serviceKey=***&LAWD_CD=11110&DEAL_YMD=202605&pageNo=1&numOfRows=1"

# 캐시 전수 스캔 (python3, 5,500 파일)
#   - schemaVersion 분포
#   - propType별 레코드 수 / area·amount·day null 카운트
#   - 최초·최종 거래일, 월별 건수, 금액 min/median/max

# dashboard.html 구간별 바이트 측정 (python3, 행 범위 슬라이싱 후 len(encode()))
```
