# 수집 스키마 변경 — 캐시 v2 → v3

- 작성일: 2026-08-12
- 대상: `/Users/kuka/Project/dashboard/ingest.mjs`, `/Users/kuka/Project/dashboard/dashboard.html`
- 배경 조사: `/Users/kuka/Project/docs/저장소-현황-조사.md`
- 상태: **재수집 완료 (2026-08-12)**. §9에 실행 결과 기록

---

## 1. 왜 바꿨나

다음 작업으로 붙일 시장 리포트 화면(동별 랜드마크 아파트의 신고가·신저가, 거래
1위 단지, 비아파트 최고가)은 **어느 단지가 얼마에 팔렸는지**를 요구한다. 그런데
`parseItems`가 API 응답에서 `{year, month, day, area, amount}` 5개만 남기고
단지명·법정동·층·건축년도·지번을 전부 버리고 있었다. 캐시 443,363건은 "언제
얼마에 팔렸다"만 알 뿐 "무엇이" 팔렸는지 모르는 상태였다.

집계 규칙은 바꾸지 않았다. 이번 변경은 **정보를 더 보존하는 것**이며, 산출물
(`data.json` / `dashboard.html`의 `#real-data`)의 내용은 한 바이트도 달라지지
않는다. §5의 검증 기준이 그것이다.

---

## 2. 무엇이 달라졌나

### 2-1. `parseItems` — 식별 필드 9개 추가

`/Users/kuka/Project/dashboard/ingest.mjs:287` `parseItems()`

**변경 전** (`ingest.mjs:251-257`, 변경 전 기준):

```javascript
    items.push({
      year:   parseInt(yearMatch[1],  10),
      month:  parseInt(monthMatch[1], 10),
      day:    dayMatch ? parseInt(dayMatch[1], 10) : null,
      area:   (area !== null && !isNaN(area))     ? area   : null,
      amount: (amount !== null && !isNaN(amount)) ? amount : null,
    });
```

**변경 후** (`/Users/kuka/Project/dashboard/ingest.mjs:316-335`):

```javascript
    items.push({
      // ── 집계 계층이 읽는 5개 필드 (aggregateItems) ──
      year:   parseInt(yearMatch[1],  10),
      month:  parseInt(monthMatch[1], 10),
      day:    dayMatch ? parseInt(dayMatch[1], 10) : null,
      area:   (area !== null && !isNaN(area))     ? area   : null,
      amount: (amount !== null && !isNaN(amount)) ? amount : null,

      // ── 캐시 계층 전용 식별 필드 (집계·화면은 읽지 않는다) ──
      // 단지·건물명: 유형마다 태그가 다르다. sh(단독/다가구)는 셋 다 없어 null.
      name:         textTag(block, 'aptNm') || textTag(block, 'mhouseNm') || textTag(block, 'offiNm'),
      umdNm:        textTag(block, 'umdNm'),
      jibun:        textTag(block, 'jibun'),
      floor:        numTag(block, 'floor'),
      buildYear:    numTag(block, 'buildYear'),
      aptSeq:       textTag(block, 'aptSeq'),
      houseType:    textTag(block, 'houseType'),
      // sh 전용 면적 대체 필드 — excluUseAr이 없는 유형의 규모 지표
      plottageAr:   numTag(block, 'plottageAr'),
      totalFloorAr: numTag(block, 'totalFloorAr'),
    });
```

보조 함수 2개를 새로 뒀다 (`/Users/kuka/Project/dashboard/ingest.mjs:233-258`):

- `textTag(block, tag)` — 텍스트 태그 1개를 읽는다. XML 엔티티(`&amp;` 등)를
  디코딩하고 `trim()` 후 빈 문자열이면 `null`. API가 미제공 필드를 **공백 1칸**으로
  주는 사례(`<aptDong> </aptDong>`, `<cdealDay> </cdealDay>`)가 실제로 있어 필요하다.
- `numTag(block, tag)` — 위를 숫자로. 파싱 실패 시 `null`.

### 2-2. 유형별 제공 현황 (2026-08-12 API 실측)

`LAWD_CD=11680&DEAL_YMD=202605` 로 4종을 각각 호출해 확인했다.

| 필드 | apt | rh | sh | offi |
|---|---|---|---|---|
| `name` | `aptNm` | `mhouseNm` | **null** (미제공) | `offiNm` |
| `umdNm` | ○ | ○ | ○ | ○ |
| `jibun` | ○ | ○ | ○ (**마스킹**: `6**`) | ○ |
| `floor` | ○ | ○ | **null** (미제공) | ○ |
| `buildYear` | ○ | ○ | ○ | ○ |
| `aptSeq` | ○ (`11680-3834`) | null | null | null |
| `houseType` | null | ○ (연립/다세대) | ○ (단독/다가구) | null |
| `plottageAr` / `totalFloorAr` | null | null | ○ | null |
| `area` (`excluUseAr`) | ○ | ○ | **null** (미제공) | ○ |

**sh(단독/다가구)의 제약이 셋이다.** 단지명 없음, 층 없음, 전용면적 없음. 지번도
`6**` 형태로 마스킹되어 위치 식별자로 쓸 수 없다. API가 주지 않는 값을 만들어내지
않았으므로 sh는 리포트의 "랜드마크 단지"에 오를 수 없고, `umdNm` + `buildYear` +
금액 + `plottageAr`/`totalFloorAr` 수준까지만 표현 가능하다.

`aptSeq`는 apt 전용 단지 고유키다. 단지명이 바뀌어도 같은 단지로 추적할 수 있어
랜드마크 화면의 조인 키로 쓴다.

### 2-3. 캐시 스키마 v2 → v3

버전 상수를 새로 도입해 저장 지점과 판정 지점이 어긋날 수 없게 했다.

`/Users/kuka/Project/dashboard/ingest.mjs:43-50`:

```javascript
// 캐시 스키마 버전 — 저장(cacheSave)과 유효성 판정(fetchCombo)이 함께 참조한다.
// 이 값을 올리면 이전 버전 캐시가 전량 무효화되어 자동 재수집된다.
//   v1: year/month/day 만
//   v2: + area / amount
//   v3: + 식별 필드(name, umdNm, jibun, floor, buildYear, aptSeq, houseType,
//        plottageAr, totalFloorAr) — parseItems 확장. 캐시 계층 전용
const CACHE_SCHEMA_VERSION = 3;
```

저장 — `/Users/kuka/Project/dashboard/ingest.mjs:366-373`:

```javascript
function cacheSave(lawdCd, dealYmd, propType, data) {
  mkdirSync(CACHE_DIR, { recursive: true });
  // schemaVersion 을 함께 저장해 구버전 캐시와 구분
  writeFileSync(
    cachePath(lawdCd, dealYmd, propType),
    JSON.stringify({ schemaVersion: CACHE_SCHEMA_VERSION, ...data }),
    'utf8'
  );
}
```

무효화 판정 — `/Users/kuka/Project/dashboard/ingest.mjs:642-646`:

```javascript
  // 캐시 확인: 현재 스키마 버전과 일치할 때만 유효한 캐시로 처리
  // 구버전 캐시(v1=날짜만, v2=area/amount까지)는 자동 무효화 → 재수집
  if (useCache) {
    const cached = cacheLoad(lawdCd, dealYmd, propType);
    if (cached !== null && cached.schemaVersion === CACHE_SCHEMA_VERSION) {
```

**재수집 대상 자동 판별**: 기존 5,600개 파일은 전부 `"schemaVersion":2`라 조건이
거짓이 되어 자동으로 재수집 대상이 된다. 별도 판별 로직을 만들지 않았다 —
메커니즘이 이미 있었고(v1→v2 전환 때 쓰인 것) 그대로 재사용했다.

파일명(`{LAWD_CD}_{YYYYMM}_{propType}.json`)은 스키마와 무관하므로 재수집은 **같은
경로를 덮어쓴다.** 새로 쌓이지 않는다.

### 2-4. `data.json` 출력 스키마는 v2 그대로

혼동하기 쉬운 지점이라 명시한다. **버전이 두 개다.**

| 버전 | 대상 | 값 | 이번 변경 |
|---|---|---|---|
| `CACHE_SCHEMA_VERSION` | `.cache/ingest/*.json` (캐시 계층) | 2 → **3** | 올림 |
| `normalized.schemaVersion` | `data.json` / `#real-data` (산출물) | **2** | 그대로 |

산출물 쪽은 `/Users/kuka/Project/dashboard/ingest.mjs:552`에서 리터럴 `2`로 남아
있고 셀프테스트(`ingest.mjs:1234`, `ingest.mjs:1323`)도 2를 검증한다. 산출물 구조가
안 바뀌었으므로 올리지 않는 것이 맞다.

### 2-5. 화면 라벨 — 금액대 차트의 집계 범위 명시

집계 로직은 **건드리지 않았다.** 표시 문구만 바꿨다.

`ingest.mjs:439-443`(현 `ingest.mjs:527-531`)의 `price` 버킷이 `rhAgg + shAgg +
offiAgg`만 합산해 아파트 229,980건(전체 51.9%)을 제외하는 것은 **의도된 설계로
확인**됐다 (근거는 §6). 그러나 화면에는 그 한정이 어디에도 표시되지 않아 사용자가
전체 거래의 금액 분포로 오해할 수 있었다. 세 곳을 고쳤다.

1. **탭 버튼** — `/Users/kuka/Project/dashboard/dashboard.html:167-169`

```html
      <button id="chartTab-price" role="tab" aria-selected="false" onclick="switchChartMode('price')"
        title="금액대 집계는 비아파트(연립다세대+단독다가구+오피스텔)만 대상으로 합니다"
        class="px-2.5 py-1 rounded-lg text-xs font-semibold text-slate-600 hover:bg-slate-100 transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-400">금액대별(비아파트)</button>
```

2. **차트 제목** — `/Users/kuka/Project/dashboard/dashboard.html:24419-24428`

```javascript
    // 차트 제목으로 집계 범위를 명시 — 이 파이는 아파트를 포함하지 않는다.
    // plugins는 얕은 전개 시 통째로 덮이므로 CHART_LIGHT_OPTS.plugins를 함께 편다.
    options = {
      ...CHART_LIGHT_OPTS,
      plugins: {
        ...CHART_LIGHT_OPTS.plugins,
        title: { display: true, text: '금액대별 (비아파트 기준)', color: '#475569', font: { size: 11 } }
      },
      scales: { x: { display: false }, y: { display: false } }
    };
```

3. **차트 하단 안내문** — `/Users/kuka/Project/dashboard/dashboard.html:24363-24372`.
   기존에는 `chartNote`가 공간규격 모드에서만 뜨는 고정 문구였다. 모드별 문구
   테이블로 바꿔 금액대 모드에서도 뜨게 했다.

```javascript
  // 모드별 집계 범위 안내 — 두 모드는 대상이 서로 다르다
  //   room:  아파트 포함 전체. 단, 단독/다가구는 전용면적 미제공으로 제외
  //   price: 비아파트(연립다세대+단독다가구+오피스텔)만. 아파트는 미포함
  const noteEl = document.getElementById('chartNote');
  const NOTES = {
    room:  '※ 단독/다가구는 전용면적 미제공으로 공간규격 집계에서 제외됩니다.',
    price: '※ 금액대 집계 대상은 비아파트(연립다세대+단독다가구+오피스텔)입니다. 아파트는 포함되지 않습니다.'
  };
  noteEl.classList.toggle('hidden', !(chartMode in NOTES));
  if (chartMode in NOTES) noteEl.textContent = NOTES[chartMode];
```

---

## 3. 화면 데이터와 캐시 데이터의 경계

**경계는 `aggregateItems`의 구조 분해 한 줄이다.**

`/Users/kuka/Project/dashboard/ingest.mjs:435-438`:

```javascript
  for (const item of items) {
    // 계층 경계: 집계는 이 5개만 읽는다. parseItems가 함께 보존하는 식별 필드
    // (name/umdNm/jibun/floor/buildYear/aptSeq/houseType/plottageAr/totalFloorAr)는
    // 캐시 계층 전용이며 여기서도, data.json에서도 쓰이지 않는다.
    const { year, month, day, area, amount } = item;
```

```
  RTMS API (XML)
        │
        ▼
  parseItems()  ── 14개 필드 파싱 ────────────────────┐
        │                                             │
        ▼                                             │
  cacheSave()   → .cache/ingest/*.json  (v3, 14개 필드 전량 보존)
        │                                             │
        ▼                                             │
  aggregateItems()  ← 5개 필드만 읽음  ────────────────┘
        │                (식별 필드는 여기서 끊긴다)
        ▼
  buildNormalized() → data.json → inject() → #real-data
                      (구·기간별 건수/버킷 카운트만. 개별 거래 없음)
```

### 왜 이 경계인가

개별 거래를 화면에 인라인하면 성립하지 않는다. 강남구 202605 실데이터(687건)를
현행 파서와 확장 파서로 각각 직렬화해 실측한 결과:

| 유형 | 표본 건수 | 현행 B/건 | v3 B/건 | 증가 |
|---|---:|---:|---:|---:|
| apt | 462 | 62.2 | 152.5 | +90.3 |
| rh | 134 | 60.6 | 145.8 | +85.1 |
| sh | 13 | 60.6 | 134.5 | +73.8 |
| offi | 78 | 60.8 | 154.4 | +93.7 |

443,363건 × 약 +88 B → **캐시 27.4MB → 약 66MB (2.4배)**. 이걸 화면에 넣으면
`dashboard.html`이 약 66MB가 되어 GitHub Pages 서빙, 브라우저 파싱, git blob
(수집 1회당 66MB 커밋) 세 곳에서 동시에 무너진다.

따라서 **원본은 캐시에만 둔다.** 리포트 화면은 다음 작업에서 캐시를 읽어 **선별된
소수 레코드**(동별 Top-N 단지, 최고가/최저가 몇 건)만 집계해 인라인한다. 개별 거래
443,363건이 화면으로 넘어가는 경로는 만들지 않는다.

---

## 4. 셀프테스트

`/Users/kuka/Project/dashboard/ingest.mjs`에 픽스처 1개와 검증 블록 1개를 추가했다.

- `FIXTURE_XML_IDENT` (`ingest.mjs:804-822`) — 2026-08-12 라이브 응답 실측을 축약.
  4유형의 태그 구성 차이를 한 픽스처에 모았고, XML 엔티티(`&amp;`)와 공백뿐인
  태그(`<jibun> </jibun>`) 케이스를 포함한다.
- `[2-b] 식별 필드 파싱 검증` (`ingest.mjs:1069-1114`) — 24개 assert.
  유형별 필드 매핑, sh의 3중 미제공(name/floor/area = null), 마스킹 지번 원문 보존,
  엔티티 디코딩, 공백→null.

**계층 경계 자체를 테스트로 고정**했다 (`ingest.mjs:1104-1112`):

```javascript
  // 계층 경계: 식별 필드가 붙어도 집계 결과는 5개 필드만 보고 계산된다
  const identAgg = aggregateItems(idItems, ['2026-06'], []);
  const identStripped = aggregateItems(
    idItems.map(({ year, month, day, area, amount }) => ({ year, month, day, area, amount })),
    ['2026-06'], []
  );
  assert('계층 경계: 식별 필드 유무로 집계가 달라지지 않음',
    JSON.stringify(identAgg) === JSON.stringify(identStripped));
```

`node dashboard/ingest.mjs --selftest` → **PASS** (신규 24건 포함 전건 통과).

---

## 5. 검증 기준 — `dashboard.html`은 라벨 변경분만 달라야 한다

이번 작업 후 `dashboard.html`의 데이터 슬롯은 **바이트 단위로 동일**해야 한다.
실측:

```
$ extract() { awk '/<script type="application\/json" id="real-data">/{f=1} f{print} /^}<\/script>/{if(f)exit}'; }
$ git show HEAD:dashboard/dashboard.html | extract | shasum
73995c3fd7767a492e1805dce3172c2b3c61ded7  -
$ extract < dashboard/dashboard.html | shasum
73995c3fd7767a492e1805dce3172c2b3c61ded7  -          ← 일치
```

| 항목 | 변경 전 | 변경 후 | 차이 |
|---|---:|---:|---:|
| `dashboard.html` 전체 | 755,536 B | 756,699 B | +1,163 B (라벨·주석만) |
| `#real-data` 슬롯 | — | — | **0 B (sha 일치)** |
| `data.json` | 476,730 B | 미변경 | 0 |

`git diff dashboard/dashboard.html` 결과도 3개 헝크뿐이며 전부 §2-5의 라벨 변경이다.
187~23,515행 데이터 구간에는 헝크가 없다.

품질 게이트 `npm run verify` → **PASS** (무효 클래스 0, 외부 리소스 2개 이하,
콘솔 에러 0, 모바일 폭 미깨짐).

puppeteer로 3개 모드를 전환하며 확인한 결과:

```
trend → note: (숨김)
room  → note: ※ 단독/다가구는 전용면적 미제공으로 공간규격 집계에서 제외됩니다.
price → note: ※ 금액대 집계 대상은 비아파트(연립다세대+단독다가구+오피스텔)입니다. 아파트는 포함되지 않습니다.
탭 라벨: 금액대별(비아파트)
콘솔 에러: 0건
```

---

## 6. 고치지 않은 것 — 아파트 금액 제외는 의도다

`ingest.mjs`의 `price` 버킷이 아파트를 제외하는 것은 **실수가 아니라 설계**로
판단했다. 서로 다른 네 곳이 일관되게 같은 말을 한다.

1. **코드 주석이 room과 price의 대상 차이를 명시적으로 대비**시킨다 —
   `/Users/kuka/Project/dashboard/ingest.mjs:514`:

```javascript
    // @MX:NOTE: [AUTO] room은 모든 유형(아파트 포함) 면적 합산, price는 비아파트(rh+sh+offi)만
```

   실수라면 이런 대비 서술이 나올 수 없다. `ingest.mjs:527`에도
   `// 금액대별: 비아파트(rh + sh + offi)만 집계 — 레이블 "금액대별(비아파트)"`.

2. **운영 문서에 기재** — `/Users/kuka/Project/dashboard/docs/실데이터_연동_가이드.md:148`:
   `아파트는 금액대 차트에서 제외(라벨이 "금액대별(비아파트)")`. 같은 문서 53행도
   `금액대 버킷(거래금액, 비아파트만)`.

3. **월간·주간 두 블록이 짝을 맞춰 동일** — `ingest.mjs:528-532`(월),
   `ingest.mjs:542-546`(주). 한 줄 누락이 아니다.

4. **목데이터 폴백이 같은 정의를 재현** —
   `/Users/kuka/Project/dashboard/dashboard.html:24088-24092`의
   `_buildMockNormalized`는 price를 `nonApt`에서만, room은 `total`(apt 포함)에서
   만든다. 실데이터 집계 정의를 목데이터가 그대로 따라간 것이다.

git 이력으로는 판정되지 않는다. `git log -S"monthlyPrice.under3" -- dashboard/ingest.mjs`는
`a1cacc1 refactor(dashboard): move dashboard artifacts into dashboard/` 한 건만
반환하며, 이는 파일 이동 커밋이라 로직 도입 시점을 담고 있지 않다. 근거는 위 4개다.

리포트용 금액 집계(아파트 포함 평균가·중앙값·최고가 등) 신설은 **다음 작업**에서
다룬다. 기존 `price` 필드를 재정의하지 말고 별도 필드로 신설해야 기존 화면과
목데이터 폴백이 깨지지 않는다.

---

## 7. API 키 보관 방식 (확인 항목)

**하드코딩 없음.** 환경변수 단일 경로다.

- `/Users/kuka/Project/dashboard/ingest.mjs:1431` — `const serviceKey = process.env.RTMS_SERVICE_KEY;`
  없으면 안내 출력 후 종료
- `/Users/kuka/Project/dashboard/ingest.mjs:606` — `url.searchParams.set('serviceKey', serviceKey);`
- `grep -rn "serviceKey *= *['\"]" /Users/kuka/Project/dashboard/` → 0건
- 실제 키는 `/Users/kuka/Project/.env` (`RTMS_SERVICE_KEY`, `ANTHROPIC_API_KEY`). gitignored 확인됨

저장소를 깃허브에 올릴 때 키가 노출될 경로는 없다. 남는 위험은 별개다 —
`ingest.mjs:96-101`의 엔드포인트 4종이 전부 `http://`라 **키가 평문 쿼리스트링으로
전송**된다. 전송 구간의 문제이며 이번 작업에서 고치지 않았다(지시에 따라 보고만).

---

## 8. 재수집 전 확인 (3단계)

**아직 실행하지 않았다.**

| 항목 | 실측/추정 |
|---|---|
| 필요 API 호출 수 | **5,601회** (5,600 콤보 + 페이지네이션 1회. 캐시 전수 파싱으로 `ceil(totalCount/1000)` 합산) |
| 일일 한도 | 공공데이터포털 일반 개발계정 **10,000건/일** (`ingest.mjs:39` 주석, `실데이터_연동_가이드.md:42`). 1회 완주로 56% 소진 |
| 오늘까지 사용량 | **약 32회** (조사 4 + 필드 확인 4 + 용량 측정 4 + 지연 측정 20). 포털이 잔여량을 API로 노출하지 않아 정확한 잔여치는 data.go.kr 마이페이지에서만 확인 가능 |
| 예상 소요 시간 | **약 35분** (실측: 20회 연속 호출 7,606ms → 건당 380ms. `DELAY_MS` 200ms + 네트워크 중앙값 176ms 포함) |
| 중간 재개 | **가능.** `fetchCombo`가 콤보 1개를 받을 때마다 즉시 `cacheSave` → 중단 시 처리분은 v3, 나머지는 v2로 남고 재실행하면 v2만 이어서 받는다 |
| 한도 초과 시 | `resultCode 22` → `isQuotaExceeded` 플래그로 재시도 없이 즉시 중단, `process.exit(1)` (`ingest.mjs:1512-1517`). `inject()` 이전이라 `dashboard.html`은 손상되지 않는다 |
| 덮어쓰기 여부 | **덮어쓴다.** 파일명이 스키마와 무관해 같은 경로에 v3가 기록된다. 되돌릴 수 없다 |
| 백업 | **완료** (아래) |

### 백업

```
/Users/kuka/Backups/seoul-dashboard/cache-ingest-v2-2026-08-12.tar.gz   3,854,477 B
/Users/kuka/Backups/seoul-dashboard/cache-ingest-v2-2026-08-12.sha256
sha256: 31df7b8ce72a533ebe19d6b9dd5ba081e694047ef5d4dc075fbb83fd3e181770
포함 파일: 5,600개 (.json) — 원본 27,892,632 B
```

저장소 밖(`/Users/kuka/Backups/`)에 뒀다. `.cache/` 안에 두면 그 디렉터리를 지울 때
백업도 함께 사라진다.

복원:

```sh
tar -xzf /Users/kuka/Backups/seoul-dashboard/cache-ingest-v2-2026-08-12.tar.gz \
    -C /Users/kuka/Project/dashboard/.cache
```

### 재수집 실행 시 (승인 후)

```sh
RTMS_SERVICE_KEY=... node /Users/kuka/Project/dashboard/ingest.mjs
```

`--fresh`는 **불필요**하다. v2 캐시가 스키마 불일치로 자동 무효화되므로 기본 실행이
곧 전량 재수집이다. `--fresh`를 쓰면 판별 로직을 우회할 뿐 결과는 같다.

### 재수집 후 검증할 것 (4단계)

1. 새 캐시 표본에 `name` / `umdNm`이 실제로 들어왔는지
2. 거래량 집계가 재수집 전과 동일한지 — `data.json`을 미리 보관해 두고 diff.
   **달라지면 안 된다** (단, 최근 2개월은 신고 지연분이 새로 들어오므로 정당한
   증가가 있다. `실데이터_연동_가이드.md:47` 참조 — 이 구간은 별도로 판단한다)
3. `dashboard.html` 최종 용량과 브라우저 로딩
4. 캐시 총량이 추정치(약 66MB) 범위인지

---

## 9. 왜곡 거래 식별 필드 추가 및 재수집 (2026-08-12)

### 9-1. 왜 추가했나

리포트의 신고가·신저가는 **왜곡 거래 3종**에 취약하다. 필요한 필드가 API 응답에
이미 들어 있는데 버리고 있었다.

| 필드 | 의미 | 왜곡 경로 |
|---|---|---|
| `cdealType` | 해제 여부 (값 `O`) | 계약해제된 거래가 응답에 그대로 남는다. 2020-02-21 이후 계약분부터 공개 |
| `cdealDay` | 해제일 | 위와 세트 |
| `dealingGbn` | 중개거래 / 직거래 | 직거래는 특수관계 거래로 시세와 동떨어질 수 있다 |
| `rgstDate` | 등기일자 | 비어 있으면 소유권이전등기 미완료 = 미확정 거래 |

`/Users/kuka/Project/dashboard/ingest.mjs:350-362` 에 보존 로직을 추가했다.
`textTag()` 가 trim 후 빈 값을 null로 처리하므로 `<cdealDay> </cdealDay>`(공백 1칸)와
자기닫힘 `<cdealType/>` 둘 다 null이 된다.

### 9-2. 유형별 제공 현황 — `rgstDate`는 apt·rh에만 있다

재수집 전 4개 구·월 표본 1,676건으로 확인한 뒤, 재수집 후 453,863건 전수로 확정했다.

| 필드 | apt | rh | sh | offi |
|---|---:|---:|---:|---:|
| `cdealType` 값 보유 | 5.31% | 5.50% | 11.08% | 5.45% |
| `dealingGbn` = 직거래 | 5.50% | 22.95% | 13.58% | 27.32% |
| `rgstDate` 공백 | 17.90% | 46.66% | **100.00%** | **100.00%** |

**sh·offi는 `rgstDate` 태그 자체가 응답에 없다.** 따라서 "등기완료" 필터를 전 유형에
일괄 적용하면 **오피스텔 56,389건과 단독·다가구 17,375건이 전량 제외**되어 리포트
3번 항목(비아파트 신고가·신저가)이 통째로 빈다.

**다음 단계의 필터는 유형별로 나눠 적용해야 한다:**

- apt · rh — `cdealType` 제외 + `dealingGbn='직거래'` 제외 + `rgstDate` 공백 제외
- sh · offi — `cdealType` 제외 + `dealingGbn='직거래'` 제외 **(등기 조건 미적용)**

이 사실은 `ingest.mjs` 의 `parseItems` JSDoc(유형별 제공 현황)과 셀프테스트
`[2-b]` 의 assert 2건(`sh rgstDate=null (태그 미제공)`,
`offi rgstDate=null (태그 미제공)`)으로 코드에 고정해 두었다.

### 9-3. 계층 경계는 그대로

`aggregateItems` 는 여전히 5개 필드만 읽는다
(`/Users/kuka/Project/dashboard/ingest.mjs:469`). 해제 거래(`cdealType='O'`)도
지금은 거래량 집계에 그대로 포함된다 — **기존 집계 방식을 바꾸지 않기 위한 의도적
선택**이며, 필터는 리포트용 집계에서만 건다.

### 9-4. 요청 페이싱 — 순차 35분 → 병렬 4.7분

**측정 과정에서 가정이 두 번 뒤집혔다.** 처음엔 동시 연결 수가 한계라고 봤으나,
같은 동시성에서 실행 순서에 따라 결과가 달라졌고(역순 재측정 시 동시성 6·8이 100%
실패), 429는 단일 요청으로 **즉시 회복**됐다. 동시성 4에서도 실패가 나오는 등
단조 관계가 아니었다.

→ 제한 대상이 **요청 간격**이라고 판단하고 속도 기준으로 재측정했다.

| 목표 속도 | 성공/실패 (각 100회) |
|---|---|
| 4 / 6 / 10 / 20 / 30 req/s | **100/0** (전부) |
| 40 req/s | 45/**55** (429) |
| 60 req/s | 35/**65** (429) |

무거운 응답(최근월 apt, 150회 38.4MB)으로도 20/s·30/s 모두 실패 0.
한계는 30~40/s 사이. **20/s를 채택** — 한계 대비 33% 여유.

구현 (`/Users/kuka/Project/dashboard/ingest.mjs:44-50`, `:638-702`):

```javascript
const REQ_PER_SEC     = 20;  // 초당 요청 시작 수 상한
const MAX_CONCURRENT  = 6;   // 동시 in-flight 요청 수 상한
const COMBO_WORKERS   = 6;   // 콤보 단위 병렬 워커 수
```

`pacer` 가 두 가지를 동시에 강제한다 — (1) 요청 **시작 간격** 최소 50ms,
(2) 동시 in-flight 6개 이하. 페이지네이션 추가 요청과 재시도도 전부 이 게이트를
지난다. `fetchOnePage` 호출을 `paced()` 로 감싸고 기존 `sleep(DELAY_MS)` 를 제거했다.
재시도는 기존 `MAX_RETRY = 3` 지수 백오프(1s/2s/4s) 그대로다.

메인 루프는 3중 중첩 for를 평탄한 작업 목록 + `runPool(tasks, COMBO_WORKERS, ...)`
로 교체했다. `runPool` 은 **입력 순서를 유지**하므로 결과 반영·교차검증 데이터
수집은 순차 시절과 동일하다.

### 9-5. 재수집 실행 결과

```
총 API 호출: 5601건 (캐시 히트: 0건)
  apt : 1401건 · rh : 1400건 · sh : 1400건 · offi : 1400건
  실패 콤보: 0건
경과: 281초 (4분 41초) · 실측 속도 20.0/s 유지 (전 구간)
```

| 항목 | 결과 |
|---|---|
| 캐시 파일 | 5,600개 전부 `"schemaVersion":3` (v2 잔존 0) |
| 총 레코드 | 443,363 → **453,863건** |
| 캐시 용량 | 27.4MB → **133.0MB** (레코드당 307B) |
| `dashboard.html` | 756,699 → **756,811 B** (+112) |
| `data.json` | 476,730 → **476,842 B** (+112) |
| `generatedAt` | 2026-08-12 |
| 품질 게이트 | PASS (콘솔 에러 0, 외부 리소스 2 이하, 모바일 미깨짐) |
| 셀프테스트 | 147건 전건 PASS |

**용량 추정이 빗나갔다.** §3에서 66MB로 추정했으나 실제는 133MB다. 추정 당시엔
필드 5개(`name`/`umd`/`jibun`/`floor`/`buildYear`)만 계산했는데 최종 스코프가 13개로
늘었기 때문이다. null 필드도 `"cdealType":null` 형태로 약 20B씩 차지한다.
디스크 여유 15GB이므로 문제는 없다.

### 9-6. C. 전수 수집 검증

**`totalCount` 대조 — 5,600개 콤보 전수, 불일치 0건.**

1회 요청 최대 반환 건수도 확인했다 (노원구 202604, `totalCount` 1,038 — 캐시 전체에서
유일한 1,000 초과 콤보):

```
numOfRows= 1000 → 반환 1000건      pageNo=1 → 1000건
numOfRows= 2000 → 반환 1038건      pageNo=2 →   38건
numOfRows= 5000 → 반환 1038건      pageNo=3 →    0건
numOfRows=10000 → 반환 1038건      누적 1038 == totalCount 1038 ✓
```

**API에 하드 상한이 없다.** 요청한 `numOfRows` 까지 그대로 준다. `PAGE_SIZE = 1000`
은 코드가 정한 값이지 API 제약이 아니다. 분할 수신은 정확히 동작한다.

### 9-7. 재수집 전후 집계 대조 — 파서가 아니라 API 데이터가 변했다

2026-05 이전 구간에서 **672건의 셀 불일치**가 나왔다. 지시서의 전제("최근 두 달만
변한다")와 다르므로 원인을 판별했다.

**판별 방법**: 백업해 둔 v2 캐시(`~/Backups/seoul-dashboard/cache-ingest-v2-2026-08-12.tar.gz`)
와 새 v3 캐시를 콤보별로 대조했다.

```
콤보 대조: 동일 5,268 / 건수 변동 332 / 신규 파일 0
총 증감: +2,438건  (증가 327콤보 +2,443 / 감소 5콤보 -5)
```

건수가 동일한 5,268개 콤보에 대해 **집계용 5개 필드만 투영해 정렬 비교**한 결과:

```
5필드 완전 동일: 5,265개 / 불일치: 3개
```

불일치 3개는 건수·`totalCount` 가 같은 채로 **레코드가 교체**된 것이다:

```
11350_202604_apt: 구(2026-04-07, 84.58㎡, 51,000) → 신(2026-04-06, 84.77㎡, 141,000)
11560_202604_offi: 금액만 190,920 → 195,000 으로 정정
```

단지·면적·금액·날짜가 전부 다른 별개 거래이거나 금액 정정이다. **파싱 산출물이
아니라 RTMS 원본이 바뀐 것이다.** 감소 5건은 파서로는 설명 불가능하고 API 삭제로만
설명된다.

계약월별 증감을 보면 최근월로 갈수록 단조 증가하는 전형적인 **지연 신고** 패턴이다:

```
2023~2024년 각 월  : +1 ~ +5건
2025-03 ~ 2025-07  : +9 ~ +39건
2026-03 / 04 / 05  : +18 / +81 / +281건
2026-06            : +1,870건   ← 직전 수집(2026-07-13) 이후 한 달치
2026-07 / 08       : 변동 없음  ← 오늘 이미 재수집된 상태였음
```

2026-05 이전 소계 **+568건** (전체의 0.13%). **"과거 확정월은 변하지 않는다"는 전제
자체가 사실이 아니다.** RTMS는 지연 신고·정정·해제를 과거 월에 계속 반영한다.
다음 작업에서 스냅샷 간 대조를 할 때 이 점을 감안해야 한다.

### 9-8. 확인하지 못한 것

- 감소 5건이 계약해제로 인한 삭제인지 단순 정정인지 — `cdealType` 이력이 캐시에
  누적되지 않아 판별 불가
- 429 제한의 정확한 정책(윈도우 길이·버킷 크기). 20/s·30/s 성공, 40/s 실패라는
  경계만 실측했다
