# 자동화 구축

- 작성일: 2026-08-13
- 목적: 프롬프트 왕복과 명령 승인을 줄이고, 백로그 → 이슈 → PR → 검토 → 검증까지
  사람이 PR 하나만 보면 되게 만든다.
- 저장소: https://github.com/byoneself4023-bit/property-workspace
  → **공개 전환 + `seoul-property-dashboard` 로 개명 예정** (§4)
- 라이브: https://byoneself4023-bit.github.io/seoul-property-dashboard/
- 로컬: `~/Project` (폴더명은 그대로 둔다)

**저장소는 하나다.** 코드도 배포물도 여기 있고, 별도 배포 저장소도 토큰도 없다.

## 진행 상태

| 단계 | 상태 | 비고 |
|---|---|---|
| 1. 승인 없애기 | **완료** | permissions 세 갈래 재구성 |
| 2. 훅 3개 | **완료** | Stop 검증 게이트 + PreToolUse 2종 |
| 3-1. push 전 안전 점검 | **완료 (통과)** | 키 이력 0건 |
| 3-2. 저장소 생성·push | **완료** | 비공개로 생성 |
| 3-3. 배포 방식 | **폐기 후 재결정** | Pages(자기 저장소). §4 |
| 4-1. 이력·파일 정리 | **완료 (검증 통과)** | 50커밋 / 26파일 / .git 852K |
| 4-2. 발행·감시 워크플로 | **완료** | `pages.yml`, `freshness.yml` |
| 4-3. 공개 전환 | **대기** | force push → 개명 → 공개 → Pages 소스 |
| 5. Actions 이관 | 미착수 | `.cache` 133MB 설계 후 |

### 폐기된 경로 두 개

기록으로 남긴다. 같은 검토를 반복하지 않기 위해서다.

- **배포 저장소 분리 + PAT** — 무료 계정이 비공개 저장소에 Pages 를 못 써서 저장소를
  둘로 나눴고, 그래서 토큰이 필요했다. 저장소를 공개로 돌리면 전제가 사라진다.
  `DEPLOY_TOKEN` 은 만들지 않았다.
- **Vercel** — 비공개 저장소를 무료로 붙일 수 있어 검토했으나, Hobby 플랜이
  **비상업적 개인 사용으로 제한**된다. "제작에 관여한 사람이 금전적 이익을 얻으면"
  상업적 사용이라 채용·계약으로 이어지는 과제물은 걸린다. 토큰 문제를 요금제
  문제와 맞바꾸는 셈이라 접었다. GitHub Pages 에는 그 제약이 없다.

---

## 1단계 — permissions

`.claude/settings.json`. 우선순위는 deny > ask > allow 이므로, 기존 allow 목록을
갈아엎지 않고 좁은 규칙을 얹어 덮는 방식을 썼다.

**allow에서 뺀 것** — `git push`, `cp`, `mv`, `gh pr:*`(너무 넓음), `rsync`

**allow에 넣은 것** (오늘 반복해서 승인했던 읽기·확인)

    Bash(git ls-files:*)  Bash(git check-ignore:*)  Bash(git rev-list:*)
    Bash(git rev-parse:*) Bash(git grep:*)          Bash(git cat-file:*)
    Bash(git fsck:*)      Bash(git shortlog:*)      Bash(git describe:*)
    Bash(git ls-remote:*) Bash(git count-objects:*)
    Bash(gh pr view:*)    Bash(gh pr list:*)        Bash(gh pr diff:*)
    Bash(gh pr checks:*)  Bash(gh pr status:*)
    Bash(gh run view:*)   Bash(gh run list:*)
    Bash(gh workflow view:*)  Bash(gh workflow list:*)
    Bash(gh api -X GET:*) Bash(gh auth status:*)
    Bash(sh -n:*)  Bash(bash -n:*)  Bash(shasum:*)  Bash(sha256sum:*)
    Bash(du:*)  Bash(stat:*)  Bash(file:*)  Bash(tr:*)  Bash(seq:*)
    Bash(date:*)  Bash(env:*)

**ask** (위험한 것만)

    Bash(git push:*)  Bash(cp:*)  Bash(mv:*)
    Bash(gh repo create:*)  Bash(gh repo delete:*)  Bash(gh repo edit:*)
    Bash(gh pr merge:*)  Bash(gh pr close:*)  Bash(gh secret:*)
    Bash(gh api -X POST:*)  Bash(gh api -X PUT:*)  Bash(gh api -X DELETE:*)
    Bash(launchctl:*)
    Write(./dashboard/data.js)  Write(./dashboard/dashboard.html)
    Write(./.gitignore)  Write(./package.json)
    Write(./dashboard/scripts/**)  Edit(./dashboard/scripts/**)

**deny**

    Read(./.env)  Read(./.env.local)  Read(./.env.production)
    Read(**/.env) Read(**/.env.local)
    Write(./.env) Edit(./.env)  Grep(./.env)  Glob(./.env)
    Read(./dashboard/.cache/**)  Write(./dashboard/.cache/**)  Edit(./dashboard/.cache/**)
    전체 스테이징 명령 2종 (-A 형태와 . 형태)

`.env.example` 은 값이 없는 템플릿이므로 어느 목록에도 넣지 않았다(읽을 수 있다).

---

## 2단계 — 훅 3개

모두 `.claude/hooks/` 에 있고 `settings.json` 에서 `$CLAUDE_PROJECT_DIR` 기준으로
호출한다. 경로를 고정하지 않았으므로 CI 러너에서도 그대로 동작한다.

    "hooks": {
      "PreToolUse": [{
        "matcher": "Bash|Write|Edit|NotebookEdit",
        "hooks": [
          {"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/block-secrets.sh\"","timeout":15},
          {"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/guard-destructive.sh\"","timeout":15}
        ]
      }],
      "Stop": [{
        "hooks": [
          {"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/verify-gate.sh\"","timeout":300}
        ]
      }]
    }

### verify-gate.sh (Stop) — 검증 게이트

변경된 파일을 보고 필요한 검증만 돌린다.

| 바뀐 파일 | 도는 검증 |
|---|---|
| `dashboard/ingest.mjs` | `node --check` + `node dashboard/ingest.mjs --selftest` |
| `dashboard.html` / `data.js` / `verify-quality.mjs` | `npm run verify` |
| `dashboard/scripts/*.sh` | `sh -n` (전체 스크립트) |
| (항상) | 추적 파일 삭제 여부, gitignore 대상이 스테이징에 들어왔는지 |

`npm run verify` 는 puppeteer로 실제 브라우저를 띄우므로 화면·데이터가 바뀐
경우에만 돈다. 매번 돌리면 느려서 게이트를 끄고 싶어진다.

실패하면 `{"decision":"block"}` 을 내어 정지를 되돌리고 계속 작업하게 한다.
**재시도 3회 상한.** 상한에 걸리면 block 대신 `systemMessage` 로 멈추고,
몇 번 시도했고 무엇이 실패했는지 남긴다. 상태는 `.claude/.state/` (gitignored).

### guard-destructive.sh (PreToolUse) — 되돌릴 수 없는 작업 가드

| 대상 | 판정 |
|---|---|
| `.cache/ingest` 삭제 | **deny** — 이 맥에만 있는 원본 45만 건, 재수집 5,500회 |
| force push | **deny** — 원격 이력 파괴 |
| `rm` / `rmdir` 일반 | ask |
| `git reset --hard`, `git clean -f`, `filter-branch/repo` | ask |
| 핵심 파일에 `>` 리다이렉트, `cp`/`mv` | ask |
| `deploy.sh`, `run-ingest.sh`, `ingest.mjs`(selftest 아님) | ask |
| 산출물 파일 Write 전체 덮어쓰기 | ask |

`npm run verify`, `--selftest`, 일반 push, 새 파일 Write 는 조용히 통과한다.

### block-secrets.sh (PreToolUse) — 비밀값 차단

- `.env` 의 실제 값과 대조. **차단 사유에는 변수 이름만 적고 값은 절대 출력하지 않는다**
- 키 형태 탐지 — Anthropic 키, GitHub 토큰, 키에 리터럴 값 대입
- Bash 전용 — `.env` 스테이징, 전체 스테이징 명령, `.env` 를 파이프/리다이렉트로
  내보내는 조합, 키 값 echo

**알려진 한계.** 훅은 Bash 명령 문자열 전체를 스캔하므로 heredoc 본문에 차단
패턴을 *문자열로* 적으면 오탐이 난다. 실제로 두 번 겪었다 — 커밋 메시지에 전체
스테이징 명령을 설명으로 적었을 때, 그리고 이 문서에 `.env` 를 읽는 예시 명령을
적었을 때. 전자는 메시지를 파일로 분리해 `git commit -F` 로, 후자는 Bash heredoc
대신 파일 쓰기 도구로 우회했다. 안전한 방향의 오탐이라 규칙을 느슨하게 하지
않고 그대로 뒀다.

---

## 3단계 — GitHub

### 3-1. 안전 점검 결과 (통과)

- `.gitignore` — `.env` 계열 3종 모두 `git check-ignore` 로 실제 차단 확인
- 추적 중인 env 파일은 `.env.example` 하나, 값 비어 있음
- **이력 전수 검색** — `.env` 실제 값 3개를 60커밋에 대해 pickaxe(`git log -S`) 0건
- 60커밋 트리 전수 패턴 검색(Anthropic 키/GitHub 토큰/키 리터럴 대입) 0건
- dangling 오브젝트 27개 개별 검사 0건
- `.cache/ingest` 133MB 추적 안 됨, `.mcp.json` 에도 값 없음

### 3-2. drive-docs 이력 제거 후 push

`drive-docs/` 에 사업 문서가 들어 있었다. 대시보드 작업에 불필요하고, 4단계에서
러너로 복제되어 에이전트 작업 트리에 들어간다. 이력까지 제거하기로 결정했다.

백업 3중 — `~/Project-backup-20260813/`

- `Project-full-copy/` (270M, 디렉터리 통째)
- `full-history.bundle` (18M)
- `drive-docs.tar.gz` (44M)

<!-- -->

    brew install git-filter-repo
    git filter-repo --path drive-docs --invert-paths --force

| | 전 | 후 |
|---|---|---|
| 추적 파일 | 61 | 26 |
| `.git` | 52M | 2.5M |
| 커밋 | 61 | 60 |
| `git log -- drive-docs` | 3건 | 0건 |

- 커밋 1개(`docs: tidy drive-docs markdown …`)가 drive-docs 만 담고 있어 빈 커밋이
  되어 가지쳐졌다. 의도한 결과다.
- filter-repo 가 마지막에 `reset --hard` 를 해서 **로컬 `drive-docs/` 파일이 실제로
  지워졌다.** 백업에서 36개 전량 복원했고 `.gitignore` 에 넣어 다시 들어오지 않게 했다.
  filter-repo 를 다시 쓸 일이 있으면 이 부작용을 기억할 것.

push 후 **원격을 새로 클론해서** 재확인 — drive-docs 흔적 0건, 키 패턴 0건,
원격 60커밋 해시가 로컬과 전부 일치. 저장소는 PRIVATE, 협업자 1명(소유자).

저장소 이름은 `seoul-property` 로 만들었다가 배포 저장소와 목록에서 혼동되어
`property-workspace` 로 변경했다. `gh repo rename` 이 로컬 원격 주소도 갱신했다.
GitHub이 옛 이름에 리다이렉트를 걸어두므로 `seoul-property` 라는 이름은
앞으로 쓰지 않는다(그 이름으로 새 저장소를 만들면 리다이렉트가 깨진다).

### 3-3. 배포 방식 — 자기 저장소 Pages (재결정)

처음에는 배포 저장소를 분리하고 Fine-grained PAT 로 밀어 넣기로 했다. 무료 계정이
비공개 저장소에 Pages 를 못 쓰기 때문이었다. 저장소를 공개로 돌리면서 그 전제가
사라져 폐기했다(§진행 상태의 "폐기된 경로 두 개").

---

## 4단계 — 공개 전환

### 4-1. 이력·현재 파일 정리 (완료)

공개는 현재 파일 26개가 아니라 **이력 전체**를 공개한다. 이력에 별도 제품
`newsletter/` 가 소스·프롬프트·회사 발신 주소까지 통째로 있었다. drive-docs 때와
같은 원칙을 한 번 더 적용했다.

백업 2중 — `~/Project-backup-20260813/pre-filter2/`
(`Project-full-copy/` 221M, `full-history.bundle` 1.9M)

**경로 제거**

    git filter-repo --force --invert-paths \
      --path newsletter --path .moai \
      --path .claude/agents/moai --path .claude/commands/moai \
      --path .claude/hooks/moai --path .claude/output-styles/moai \
      --path .claude/rules/moai --path-glob '.claude/skills/moai*' \
      --path 'docs/전체플랫폼_스택제안.md' \
      --mailmap <mailmap>

**blob 내용 치환** — 경로를 지워도 다른 문서가 인용한 문자열은 남는다.
특허 출원번호·문서 제목이 조사 문서 2곳에 남아 있어 별도로 치환했다.

    git filter-repo --force --replace-text <replace-text>

| | 전 | 후 |
|---|---|---|
| 커밋 | 62 | **50** |
| 추적 파일 | 27 | **26** |
| `.git` | 2.6M | **852K** |
| 이력상 고유 경로 | **589** | **34** |

검증 — 제거 대상 9종 전 커밋 트리 전수검색 0건, 사업 용어·특허번호·문서제목
이력 blob 검색 0건, 로컬 `drive-docs/` 36개와 `.cache/` 144MB 와 `.env` 전부 보존.

**커밋 이메일**을 `223078465+byoneself4023-bit@users.noreply.github.com` 로 바꿨다
(50커밋 작성자·커미터 전부, `deploy.sh` 안의 하드코딩 포함).

현재 파일 정리 — `CLAUDE.md` 의 "public 이면 안 된다" 교체, 조사 문서의 특허
문서 제목·번호 삭제, `settings.json` PATH 에서 홈 경로 제거, `docs/*.md` 의
절대경로 146곳을 `~/` 로 치환.

### 4-2. 발행·감시 워크플로 (완료)

**`.github/workflows/pages.yml`** — `main` 에 화면·데이터가 푸시되면 두 파일만
`_site/` 로 조립해 Pages 로 올린다. 저장소 전체가 서빙되지 않는다.

- `dashboard.html` → `index.html` 개명은 여기서 한다. 저장소의 파일명을 바꾸면
  `ingest.mjs` 의 `inject()` 가 깨지므로 원본은 그대로 둔다
- 발행 전 점검 — 두 파일이 비지 않았는지, `index.html` 에 `data.js` 참조가 있는지,
  `data.js` 에 전역 할당이 있는지. 하나만 올라가 목데이터로 뜨는 것을 막는다
- `paths` 필터로 문서만 고친 커밋은 배포를 트리거하지 않는다
- `concurrency: pages`, `cancel-in-progress: false` — 반쯤 올라간 상태를 만들지 않는다

**`.github/workflows/freshness.yml`** — 이번 전환의 핵심.

4주간 라이브가 멈춘 걸 아무도 몰랐던 이유는 알림 부재가 아니라 **돌지 않은 작업은
실패하지도 않는다**는 데 있다. 수집이 죽으면 실패 알림도 안 온다. 그래서 수집과
독립된 감시를 둔다.

- 매일 09:00 KST, 라이브의 `data.js` 에서 `generatedAt` 을 읽어 **10일** 넘으면 실패
- 화면·데이터 각각 HTTP 200 도 확인 — `data.js` 만 404 면 목데이터로 뜬다
- 실패하면 **이슈를 자동 생성**한다. 메일은 묻히지만 이슈는 저장소를 열면 보인다.
  열린 이슈가 있으면 중복 생성하지 않는다
- 공개 저장소는 60일 무활동 시 스케줄이 자동 비활성화된다. 이 감시가 매일 돌면
  그 전에 사람이 알아챈다

두 워크플로 모두 `workflow_dispatch` 를 열어뒀다.

**메일 알림 설정** (1회, 웹) — GitHub Settings → Notifications → Actions →
드롭다운을 **"Only notify for failed workflows"** 로. 스케줄 워크플로의 알림은
그 워크플로를 만든 사용자에게 간다.

### 4-3. `deploy.sh` 축소 (완료)

자기 저장소 발행이라 114줄 → **89줄**. `gh` 의존이 통째로 사라졌다.

걷어낸 것 — 배포 저장소 clone/fetch/reset, `gh auth setup-git`, 배포 저장소로의
별도 commit/push, Pages 빌드 상태 폴링, `OWNER`/`REPO` 상수.

남긴 것 — `generatedAt` 추출, no-op 스킵(빈 커밋 방지), **두 파일을 항상 함께
다루는 규칙**, 그리고 **라이브 검증**(화면·데이터 각각 HTTP 200 + sha256 일치).
라이브 검증이 가장 중요하다. Pages 로 남는다고 4주 침묵의 위험이 사라지지 않는다.

라이브 주소는 `origin` 에서 유도한다 — 저장소 이름이 바뀌어도 따라간다.
`sed -E` 의 lazy 수량자가 BSD(macOS)에서 안 되므로 파라미터 확장만 썼다.

`run-ingest.sh` 는 그대로다. 수집 성공(rc=0)일 때만 배포하는 구조가 유효하다.

> **앞으로 없어질 수 있다.** 수집을 GitHub Actions 로 옮기면(`.cache` 133MB 설계가
> 정해진 뒤) 워크플로가 수집·커밋·푸시를 모두 하므로 `deploy.sh` 와 `run-ingest.sh`
> 둘 다 사라진다. 지금 축소한 것은 그때까지의 다리다.

### 4-4. 공개 전환 순서 (대기)

이력을 재작성했으므로 원격과 갈라져 있다. 순서를 지켜야 라이브 공백이 최소가 된다.

1. **force push** — 로컬 50커밋으로 원격 61커밋을 덮는다.
   훅과 permissions 가 force push 를 `deny` 로 막고 있어 사람이 직접 실행해야 한다
2. 기존 `seoul-property-dashboard` 를 다른 이름으로 rename (삭제하지 않는다)
3. `property-workspace` → `seoul-property-dashboard` 로 rename.
   **rename 이 두 번째이므로 리다이렉트 체인이 꼬이지 않는지 확인한다**
   (`seoul-property` → `property-workspace` 리다이렉트가 이미 걸려 있다)
4. Settings → General → **공개로 전환**
5. Settings → Pages → Source: **GitHub Actions**
6. `workflow_dispatch` 로 발행 워크플로 수동 실행
7. 라이브 확인 — 화면 200, `data.js` 200, 수집일 2026-08-12
8. `freshness.yml` 도 수동 실행해 통과하는지 확인

주소는 지금과 동일하게 유지된다:
`https://byoneself4023-bit.github.io/seoul-property-dashboard/`
대표님께 새 주소를 안내할 필요가 없다.

---

## 남은 일

- **`ANTHROPIC_API_KEY` 등록** — 이 전환과 무관하게 여전히 필요하다.
  `.env` 읽기를 deny 로 막아 자동 등록이 불가하므로 사람이 직접 실행한다.
  값이 `sk-ant-oat` 로 시작하는 구독 OAuth 토큰이면 CI 에서 거부되므로
  `sk-ant-api` 로 시작하는 콘솔 키여야 한다.

      gh secret set ANTHROPIC_API_KEY -R byoneself4023-bit/seoul-property-dashboard

- **`@claude` 워크플로** — 이슈·PR 에서 에이전트를 부르는 자동화. 위 키가 있어야 한다
- **수집 Actions 이관** — `.cache/ingest` 133MB 를 저장소에 넣을지, Actions 캐시를
  쓸지, 매번 최근 두 달만 받을지. 정본을 로컬로 볼지 러너로 볼지도 함께 정해야 한다
- **기존 배포 저장소 정리** — 새 배포 확인 후 안내 페이지로 바꾸고 3개월 뒤 아카이브
- **`com.newsletter.*` LaunchAgent 2개** — 제거된 제품을 계속 실행하려다 실패하며
  로그를 쌓고 있다(`ui-server.log` 58MB, MODULE_NOT_FOUND 78,850회).
  이 저장소 밖의 문제지만 같이 정리하는 게 좋다

## 실패 시 확인 경로

| 무엇 | 어디 |
|---|---|
| 라이브가 낡았는지 | Issues 탭 — `데이터-정지` 라벨이 붙은 이슈 |
| 신선도 감시 로그 | `gh run list --workflow=freshness.yml` |
| 발행 실패 | `gh run list --workflow=pages.yml` → `gh run view <id> --log-failed` |
| 로컬 수집 로그 | `dashboard/logs/ingest-launchd.log` (직전 세대는 `.prev`) |
| launchd 살아 있는지 | `launchctl list \| grep dashboard-ingest` |
| Stop 훅 검증 로그 | `.claude/.state/verify-<session>.log` |
| 훅이 발동했는지 | 세션에서 `/hooks` |
| 이력 재작성 되돌리기 | `~/Project-backup-20260813/pre-filter2/Project-full-copy/` |

**키 값은 이 문서 어디에도 적지 않는다.**
