# 자동화 구축

- 작성일: 2026-08-13
- 목적: 프롬프트 왕복과 명령 승인을 줄이고, 백로그 → 이슈 → PR → 검토 → 검증까지
  사람이 PR 하나만 보면 되게 만든다.
- 저장소: https://github.com/byoneself4023-bit/seoul-property-dashboard (**공개**)
- 라이브: https://byoneself4023-bit.github.io/seoul-property-dashboard/
- 로컬: `~/Project` (폴더명은 그대로 둔다)

**저장소는 하나다.** 코드도 배포물도 여기 있고, 별도 배포 저장소도 토큰도 없다.

## 진행 상태 — 구축 완료 (2026-08-13)

| 단계 | 상태 | 비고 |
|---|---|---|
| 1. 승인 없애기 | **완료** | permissions 세 갈래 재구성 |
| 2. 훅 3개 | **완료** | Stop 검증 게이트 + PreToolUse 2종 |
| 3. 저장소 생성·push | **완료** | 안전 점검 통과 후 생성 |
| 4-1. 이력·파일 정리 | **완료** | 50커밋 / 이력상 경로 589→34 / .git 852K |
| 4-2. 발행·감시 워크플로 | **완료** | `pages.yml`, `freshness.yml` |
| 4-3. 에이전트 워크플로 | **완료** | `claude.yml`, `verify.yml`, `auto-pr.yml` |
| 4-4. 공개 전환 + Pages | **완료** | 라이브 복구 (수집일 2026-08-12) |
| 4-5. 브랜치 보호 | **완료** | ruleset `main 보호` |
| 5. 수집 Actions 이관 | 미착수 | `.cache` 133MB 설계 후 |

### 폐기된 경로 두 개

기록으로 남긴다. 같은 검토를 반복하지 않기 위해서다.

- **배포 저장소 분리 + PAT** — 무료 계정이 비공개 저장소에 Pages 를 못 써서 저장소를
  둘로 나눴고, 그래서 토큰이 필요했다. 저장소를 공개로 돌리면 전제가 사라진다.
  `DEPLOY_TOKEN` 은 만들지 않았다.
- **Vercel** — 비공개 저장소를 무료로 붙일 수 있어 검토했으나, Hobby 플랜이
  **비상업적 개인 사용으로 제한**된다. "제작에 관여한 사람이 금전적 이익을 얻으면"
  상업적 사용이라 채용·계약으로 이어지는 과제물은 걸린다. 토큰 문제를 요금제
  문제와 맞바꾸는 셈이라 접었다. GitHub Pages 에는 그 제약이 없다.

---

## 1단계 — permissions

`.claude/settings.json`. 우선순위는 deny > ask > allow 이므로, 기존 allow 목록을
갈아엎지 않고 좁은 규칙을 얹어 덮는 방식을 썼다.

**allow에서 뺀 것** — `git push`, `cp`, `mv`, `gh pr:*`(너무 넓음), `rsync`

**allow에 넣은 것** (오늘 반복해서 승인했던 읽기·확인)

    Bash(git ls-files:*)  Bash(git check-ignore:*)  Bash(git rev-list:*)
    Bash(git rev-parse:*) Bash(git grep:*)          Bash(git cat-file:*)
    Bash(git fsck:*)      Bash(git shortlog:*)      Bash(git describe:*)
    Bash(git ls-remote:*) Bash(git count-objects:*)
    Bash(gh pr view:*)    Bash(gh pr list:*)        Bash(gh pr diff:*)
    Bash(gh pr checks:*)  Bash(gh pr status:*)
    Bash(gh run view:*)   Bash(gh run list:*)
    Bash(gh workflow view:*)  Bash(gh workflow list:*)
    Bash(gh api -X GET:*) Bash(gh auth status:*)
    Bash(sh -n:*)  Bash(bash -n:*)  Bash(shasum:*)  Bash(sha256sum:*)
    Bash(du:*)  Bash(stat:*)  Bash(file:*)  Bash(tr:*)  Bash(seq:*)
    Bash(date:*)  Bash(env:*)

**ask** (위험한 것만)

    Bash(git push:*)  Bash(cp:*)  Bash(mv:*)
    Bash(gh repo create:*)  Bash(gh repo delete:*)  Bash(gh repo edit:*)
    Bash(gh pr merge:*)  Bash(gh pr close:*)  Bash(gh secret:*)
    Bash(gh api -X POST:*)  Bash(gh api -X PUT:*)  Bash(gh api -X DELETE:*)
    Bash(launchctl:*)
    Write(./dashboard/data.js)  Write(./dashboard/dashboard.html)
    Write(./.gitignore)  Write(./package.json)
    Write(./dashboard/scripts/**)  Edit(./dashboard/scripts/**)

**deny**

    Read(./.env)  Read(./.env.local)  Read(./.env.production)
    Read(**/.env) Read(**/.env.local)
    Write(./.env) Edit(./.env)  Grep(./.env)  Glob(./.env)
    Read(./dashboard/.cache/**)  Write(./dashboard/.cache/**)  Edit(./dashboard/.cache/**)
    전체 스테이징 명령 2종 (-A 형태와 . 형태)

`.env.example` 은 값이 없는 템플릿이므로 어느 목록에도 넣지 않았다(읽을 수 있다).

---

## 2단계 — 훅 3개

모두 `.claude/hooks/` 에 있고 `settings.json` 에서 `$CLAUDE_PROJECT_DIR` 기준으로
호출한다. 경로를 고정하지 않았으므로 CI 러너에서도 그대로 동작한다.

    "hooks": {
      "PreToolUse": [{
        "matcher": "Bash|Write|Edit|NotebookEdit",
        "hooks": [
          {"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/block-secrets.sh\"","timeout":15},
          {"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/guard-destructive.sh\"","timeout":15}
        ]
      }],
      "Stop": [{
        "hooks": [
          {"type":"command","command":"bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/verify-gate.sh\"","timeout":300}
        ]
      }]
    }

### verify-gate.sh (Stop) — 검증 게이트

변경된 파일을 보고 필요한 검증만 돌린다.

| 바뀐 파일 | 도는 검증 |
|---|---|
| `dashboard/ingest.mjs` | `node --check` + `node dashboard/ingest.mjs --selftest` |
| `dashboard.html` / `data.js` / `verify-quality.mjs` | `npm run verify` |
| `dashboard/scripts/*.sh` | `sh -n` (전체 스크립트) |
| (항상) | 추적 파일 삭제 여부, gitignore 대상이 스테이징에 들어왔는지 |

`npm run verify` 는 puppeteer로 실제 브라우저를 띄우므로 화면·데이터가 바뀐
경우에만 돈다. 매번 돌리면 느려서 게이트를 끄고 싶어진다.

실패하면 `{"decision":"block"}` 을 내어 정지를 되돌리고 계속 작업하게 한다.
**재시도 3회 상한.** 상한에 걸리면 block 대신 `systemMessage` 로 멈추고,
몇 번 시도했고 무엇이 실패했는지 남긴다. 상태는 `.claude/.state/` (gitignored).

### guard-destructive.sh (PreToolUse) — 되돌릴 수 없는 작업 가드

| 대상 | 판정 |
|---|---|
| `.cache/ingest` 삭제 | **deny** — 이 맥에만 있는 원본 45만 건, 재수집 5,500회 |
| force push | **deny** — 원격 이력 파괴 |
| `rm` / `rmdir` 일반 | ask |
| `git reset --hard`, `git clean -f`, `filter-branch/repo` | ask |
| 핵심 파일에 `>` 리다이렉트, `cp`/`mv` | ask |
| `deploy.sh`, `run-ingest.sh`, `ingest.mjs`(selftest 아님) | ask |
| 산출물 파일 Write 전체 덮어쓰기 | ask |

`npm run verify`, `--selftest`, 일반 push, 새 파일 Write 는 조용히 통과한다.

### block-secrets.sh (PreToolUse) — 비밀값 차단

- `.env` 의 실제 값과 대조. **차단 사유에는 변수 이름만 적고 값은 절대 출력하지 않는다**
- 키 형태 탐지 — Anthropic 키, GitHub 토큰, 키에 리터럴 값 대입
- Bash 전용 — `.env` 스테이징, 전체 스테이징 명령, `.env` 를 파이프/리다이렉트로
  내보내는 조합, 키 값 echo

**알려진 한계.** 훅은 Bash 명령 문자열 전체를 스캔하므로 heredoc 본문에 차단
패턴을 *문자열로* 적으면 오탐이 난다. 실제로 두 번 겪었다 — 커밋 메시지에 전체
스테이징 명령을 설명으로 적었을 때, 그리고 이 문서에 `.env` 를 읽는 예시 명령을
적었을 때. 전자는 메시지를 파일로 분리해 `git commit -F` 로, 후자는 Bash heredoc
대신 파일 쓰기 도구로 우회했다. 안전한 방향의 오탐이라 규칙을 느슨하게 하지
않고 그대로 뒀다.

---

## 3단계 — GitHub

### 3-1. 안전 점검 결과 (통과)

- `.gitignore` — `.env` 계열 3종 모두 `git check-ignore` 로 실제 차단 확인
- 추적 중인 env 파일은 `.env.example` 하나, 값 비어 있음
- **이력 전수 검색** — `.env` 실제 값 3개를 60커밋에 대해 pickaxe(`git log -S`) 0건
- 60커밋 트리 전수 패턴 검색(Anthropic 키/GitHub 토큰/키 리터럴 대입) 0건
- dangling 오브젝트 27개 개별 검사 0건
- `.cache/ingest` 133MB 추적 안 됨, `.mcp.json` 에도 값 없음

### 3-2. drive-docs 이력 제거 후 push

`drive-docs/` 에 사업 문서가 들어 있었다. 대시보드 작업에 불필요하고, 4단계에서
러너로 복제되어 에이전트 작업 트리에 들어간다. 이력까지 제거하기로 결정했다.

백업 3중 — `~/Project-backup-20260813/`

- `Project-full-copy/` (270M, 디렉터리 통째)
- `full-history.bundle` (18M)
- `drive-docs.tar.gz` (44M)

<!-- -->

    brew install git-filter-repo
    git filter-repo --path drive-docs --invert-paths --force

| | 전 | 후 |
|---|---|---|
| 추적 파일 | 61 | 26 |
| `.git` | 52M | 2.5M |
| 커밋 | 61 | 60 |
| `git log -- drive-docs` | 3건 | 0건 |

- 커밋 1개(`docs: tidy drive-docs markdown …`)가 drive-docs 만 담고 있어 빈 커밋이
  되어 가지쳐졌다. 의도한 결과다.
- filter-repo 가 마지막에 `reset --hard` 를 해서 **로컬 `drive-docs/` 파일이 실제로
  지워졌다.** 백업에서 36개 전량 복원했고 `.gitignore` 에 넣어 다시 들어오지 않게 했다.
  filter-repo 를 다시 쓸 일이 있으면 이 부작용을 기억할 것.

push 후 **원격을 새로 클론해서** 재확인 — drive-docs 흔적 0건, 키 패턴 0건,
원격 60커밋 해시가 로컬과 전부 일치. 저장소는 PRIVATE, 협업자 1명(소유자).

저장소 이름은 `seoul-property` 로 만들었다가 배포 저장소와 목록에서 혼동되어
`property-workspace` 로 변경했다. `gh repo rename` 이 로컬 원격 주소도 갱신했다.
GitHub이 옛 이름에 리다이렉트를 걸어두므로 `seoul-property` 라는 이름은
앞으로 쓰지 않는다(그 이름으로 새 저장소를 만들면 리다이렉트가 깨진다).

### 3-3. 배포 방식 — 자기 저장소 Pages (재결정)

처음에는 배포 저장소를 분리하고 Fine-grained PAT 로 밀어 넣기로 했다. 무료 계정이
비공개 저장소에 Pages 를 못 쓰기 때문이었다. 저장소를 공개로 돌리면서 그 전제가
사라져 폐기했다(§진행 상태의 "폐기된 경로 두 개").

---

## 4단계 — 공개 전환

### 4-1. 이력·현재 파일 정리 (완료)

공개는 현재 파일 26개가 아니라 **이력 전체**를 공개한다. 이력에 별도 제품
`newsletter/` 가 소스·프롬프트·회사 발신 주소까지 통째로 있었다. drive-docs 때와
같은 원칙을 한 번 더 적용했다.

백업 2중 — `~/Project-backup-20260813/pre-filter2/`
(`Project-full-copy/` 221M, `full-history.bundle` 1.9M)

**경로 제거**

    git filter-repo --force --invert-paths \
      --path newsletter --path .moai \
      --path .claude/agents/moai --path .claude/commands/moai \
      --path .claude/hooks/moai --path .claude/output-styles/moai \
      --path .claude/rules/moai --path-glob '.claude/skills/moai*' \
      --path 'docs/전체플랫폼_스택제안.md' \
      --mailmap <mailmap>

**blob 내용 치환** — 경로를 지워도 다른 문서가 인용한 문자열은 남는다.
특허 출원번호·문서 제목이 조사 문서 2곳에 남아 있어 별도로 치환했다.

    git filter-repo --force --replace-text <replace-text>

| | 전 | 후 |
|---|---|---|
| 커밋 | 62 | **50** |
| 추적 파일 | 27 | **26** |
| `.git` | 2.6M | **852K** |
| 이력상 고유 경로 | **589** | **34** |

검증 — 제거 대상 9종 전 커밋 트리 전수검색 0건, 사업 용어·특허번호·문서제목
이력 blob 검색 0건, 로컬 `drive-docs/` 36개와 `.cache/` 144MB 와 `.env` 전부 보존.

**커밋 이메일**을 `223078465+byoneself4023-bit@users.noreply.github.com` 로 바꿨다
(50커밋 작성자·커미터 전부, `deploy.sh` 안의 하드코딩 포함).

현재 파일 정리 — `CLAUDE.md` 의 "public 이면 안 된다" 교체, 조사 문서의 특허
문서 제목·번호 삭제, `settings.json` PATH 에서 홈 경로 제거, `docs/*.md` 의
절대경로 146곳을 `~/` 로 치환.

### 4-2. 발행·감시 워크플로 (완료)

**`.github/workflows/pages.yml`** — `main` 에 화면·데이터가 푸시되면 두 파일만
`_site/` 로 조립해 Pages 로 올린다. 저장소 전체가 서빙되지 않는다.

- `dashboard.html` → `index.html` 개명은 여기서 한다. 저장소의 파일명을 바꾸면
  `ingest.mjs` 의 `inject()` 가 깨지므로 원본은 그대로 둔다
- 발행 전 점검 — 두 파일이 비지 않았는지, `index.html` 에 `data.js` 참조가 있는지,
  `data.js` 에 전역 할당이 있는지. 하나만 올라가 목데이터로 뜨는 것을 막는다
- `paths` 필터로 문서만 고친 커밋은 배포를 트리거하지 않는다
- `concurrency: pages`, `cancel-in-progress: false` — 반쯤 올라간 상태를 만들지 않는다

**`.github/workflows/freshness.yml`** — 이번 전환의 핵심.

4주간 라이브가 멈춘 걸 아무도 몰랐던 이유는 알림 부재가 아니라 **돌지 않은 작업은
실패하지도 않는다**는 데 있다. 수집이 죽으면 실패 알림도 안 온다. 그래서 수집과
독립된 감시를 둔다.

- 매일 09:00 KST, 라이브의 `data.js` 에서 `generatedAt` 을 읽어 **10일** 넘으면 실패
- 화면·데이터 각각 HTTP 200 도 확인 — `data.js` 만 404 면 목데이터로 뜬다
- 실패하면 **이슈를 자동 생성**한다. 메일은 묻히지만 이슈는 저장소를 열면 보인다.
  열린 이슈가 있으면 중복 생성하지 않는다
- 공개 저장소는 60일 무활동 시 스케줄이 자동 비활성화된다. 이 감시가 매일 돌면
  그 전에 사람이 알아챈다

두 워크플로 모두 `workflow_dispatch` 를 열어뒀다.

**메일 알림 설정** (1회, 웹) — GitHub Settings → Notifications → Actions →
드롭다운을 **"Only notify for failed workflows"** 로. 스케줄 워크플로의 알림은
그 워크플로를 만든 사용자에게 간다.

### 4-3. `deploy.sh` 축소 (완료)

자기 저장소 발행이라 114줄 → **89줄**. `gh` 의존이 통째로 사라졌다.

걷어낸 것 — 배포 저장소 clone/fetch/reset, `gh auth setup-git`, 배포 저장소로의
별도 commit/push, Pages 빌드 상태 폴링, `OWNER`/`REPO` 상수.

남긴 것 — `generatedAt` 추출, no-op 스킵(빈 커밋 방지), **두 파일을 항상 함께
다루는 규칙**, 그리고 **라이브 검증**(화면·데이터 각각 HTTP 200 + sha256 일치).
라이브 검증이 가장 중요하다. Pages 로 남는다고 4주 침묵의 위험이 사라지지 않는다.

라이브 주소는 `origin` 에서 유도한다 — 저장소 이름이 바뀌어도 따라간다.
`sed -E` 의 lazy 수량자가 BSD(macOS)에서 안 되므로 파라미터 확장만 썼다.

`run-ingest.sh` 는 그대로다. 수집 성공(rc=0)일 때만 배포하는 구조가 유효하다.

> **앞으로 없어질 수 있다.** 수집을 GitHub Actions 로 옮기면(`.cache` 133MB 설계가
> 정해진 뒤) 워크플로가 수집·커밋·푸시를 모두 하므로 `deploy.sh` 와 `run-ingest.sh`
> 둘 다 사라진다. 지금 축소한 것은 그때까지의 다리다.

### 4-4. 에이전트 워크플로 (완료)

이슈·PR 코멘트에 `@claude` 를 언급하면 에이전트가 조사·구현해 브랜치를 올리고
Draft PR 이 열린다. 인증은 `ANTHROPIC_API_KEY` 시크릿(콘솔 API 키)이다.
구독 OAuth 토큰(`sk-ant-oat…`)은 쓰지 않는다 — `anthropic_api_key` 입력에 넣으면
`Invalid API key` 로 거부된다.

| 파일 | 역할 |
|---|---|
| `claude.yml` | `@claude` 트리거. 에이전트 실행 후 브랜치 확인 → 품질 게이트 → Draft PR |
| `verify.yml` | 사람이 여는 PR과 수동 실행용 품질 게이트 |
| `auto-pr.yml` | 백스톱. 사람이 `claude/**` 를 push 하거나 수동 실행할 때 |
| `.github/scripts/open-draft-pr.sh` | PR 생성 로직 (두 워크플로가 공유) |
| `.github/claude-ci-settings.json` | CI 전용 permissions + 훅 3개 |

**러너 환경에서 실제로 걸린 것 두 개.** 문서로는 안 보이고 돌려봐야 나왔다.

- `puppeteer` 25 의 `executablePath()` 가 **Promise 를 반환**한다. `await` 없이
  `test -x` 에 넣어 항상 실패했다 — 환경 문제가 아니라 검사 코드 오류였다
- **Ubuntu 23.10+ 가 AppArmor 로 비특권 user namespace 를 막아** 크롬 샌드박스가
  뜨지 못했다(`No usable sandbox`). `verify-quality.mjs` 에 `--no-sandbox` 를 넣는
  대신 러너 쪽 제한(`kernel.apparmor_restrict_unprivileged_userns=0`)을 풀었다.
  로컬(macOS)과 CI 가 같은 조건에서 같은 게이트를 돌아야 게이트가 의미를 갖는다

**CI 에는 물어볼 사람이 없다.** 로컬 `settings.json` 의 `ask` 규칙이 CI 에서는 곧
거부가 되고, 그 거부가 재시도 루프를 만든다. 실제로 에이전트가 `gh api -X PATCH` 로
코멘트를 갱신하려다 8번 막히며 20턴을 소진하고 죽었다. `claude-ci-settings.json` 을
분리해 `ask` 를 0개로 두고 필요한 것만 allow, 위험한 것은 deny 로 못박았다.
**훅 3개는 로컬과 바이트 단위로 동일하게 싣는다** — 방어선은 그대로다.

**에이전트의 도구 선택은 신뢰하지 않는다.** 허용된 `mcp__github_comment__update_claude_comment`
를 두고 `gh api` 로만 8번 시도한 전례가 있다. 그래서 PR 생성·검증을 프롬프트로
지시하지 않고 **셸 단계**로 처리한다.

**에이전트가 실패해도 뒤 단계는 돈다.** 턴 소진으로 죽어도 커밋·푸시는 이미 끝났을
수 있으므로 `if: ${{ !cancelled() }}` 를 쓴다. 단 브랜치가 없으면 `found=false` 로
걸러져 PR 이 만들어지지 않는다. 품질 게이트에는 `continue-on-error` 를 걸어
게이트 실패가 PR 생성을 막지 않게 했다 — 실패한 채로 보이는 편이 침묵보다 낫다.

**PR 은 액션이 만들지 않는다.** `claude-code-action` 은 설계상 브랜치만 올리고
"Create PR" 링크를 남긴다(FAQ: 브랜치 보호 준수와 사람의 최종 판단). 그래서
PR 생성을 우리가 한다. 중복은 **브랜치 단위 + 이슈 단위** 두 겹으로 막는다 —
같은 이슈의 열린 PR 이 있으면 만들지 않고 기존 PR 에 새 브랜치를 알린다.

**막혔던 설정 두 개**

- `Actions is not permitted to create or approve pull requests` →
  `can_approve_pull_request_reviews: true` 로 변경. 이 체크박스는 생성과 승인을
  함께 허용하는데, 아래 브랜치 보호에서 승인을 요구하지 않으므로 무해하다
- 에이전트의 push 는 **`github-actions[bot]`(id 41898282)** 이름으로 나간다.
  진짜 `claude[bot]`(id 209825114) 이 아니다. GitHub 은 이 봇이 만든 이벤트로
  워크플로를 트리거하지 않으므로 **브랜치 push 에 반응하는 워크플로는 영영
  돌지 않는다.** 그래서 검증·PR 생성을 에이전트와 같은 잡으로 옮겼다

### 4-5. 공개 전환 + Pages (완료)

공개 직전 이력 전체를 다시 훑어 키 패턴 0건, 옛 이메일 0건, 사업 용어 0건,
작성자 단일 noreply 를 확인하고 전환했다.

    gh repo edit --visibility public --accept-visibility-change-consequences
    gh api -X POST repos/.../pages -f build_type=workflow

**Pages 설정은 웹 UI 없이 API 로 됐다.** 활성화와 소스 지정이 한 번에 처리된다.

라이브 검증 — `index.html` HTTP 200(280,569 B), `data.js` HTTP 200(477,001 B),
수집일 **2026-08-12**. 라이브 `index.html` 의 `?v=4a14749a` 와 라이브 `data.js` 의
sha256 앞 8자가 일치하고, 로컬 파일과도 sha256 전체가 같다. **화면과 데이터가
어긋난 조합이 올라간 게 아니다.**

`freshness.yml` 도 라이브를 읽고 통과했다 — "수집일 2026-08-12, 1일 경과(상한 10일)".

공개 전환으로 얻은 것: Actions 사용량 무제한, 브랜치 보호·ruleset 사용 가능,
Draft PR 제약 해소.

### 4-6. 브랜치 보호 (완료)

ruleset `main 보호` — `~DEFAULT_BRANCH` 대상, `enforcement: active`.

| 규칙 | 값 |
|---|---|
| Require a pull request before merging | 켜기, **required approvals = 0** |
| Block force pushes (`non_fast_forward`) | 켜기 |
| Restrict deletions (`deletion`) | 켜기 |
| Required status checks | **걸지 않음** |
| Bypass actors | Repository admin (`actor_id: 5`), `bypass_mode: always` |

**승인을 요구하지 않는 것이 핵심이다.** GitHub 문서에 "Pull request authors cannot
approve their own pull requests" 라고 명시돼 있다. 1인 저장소에서 승인을 1건이라도
요구하면 본인 PR 을 본인이 승인할 수 없어 영원히 병합하지 못한다. 유일한 탈출구가
봇 승인인데 그게 곧 자가 승인 구멍이다. **요구 승인이 0이면 봇이 승인해도 얻는 게
없으므로 구멍의 전제가 사라진다.**

**required status checks 를 걸지 않은 이유** — `verify.yml` 은 `paths` 필터가 있어
문서만 바뀐 PR 에서는 아예 실행되지 않는다. required 로 걸면 그런 PR 이 "대기 중"에서
영원히 멈춘다. **조건부 실행 워크플로는 required 로 걸지 않는다.**

이 보호가 실제로 막는 것 — 에이전트가 `main` 에 직접 push 하는 것, force push,
`main` 삭제. bypass 에 관리자가 있으므로 로컬에서 `main` 직접 push 는 그대로 된다.

되돌리려면:

    gh api -X DELETE repos/byoneself4023-bit/seoul-property-dashboard/rulesets/20787745

---

## 5단계 — 수집을 Actions 로 이관 (완료)

4주 침묵의 근본 원인은 라이브가 아니라 **수집**이었다. 라이브를 살려도 수집이
로컬 launchd 에 있는 한 맥이 꺼진 주는 똑같이 멈춘다.

### 캐시 133MB 를 어디에 둘지 — A(저장소) 선택

| | 저장소 편입 (A) | Actions 캐시 (B) | 캐시 없이 병합 (C) |
|---|---|---|---|
| 저장소 크기 | 최초 +14.8MB, 주당 +0.24MB | 영향 없음 | 영향 없음 |
| 주간 API 호출 | 200건 | 200건 (적중 시) | 200건 |
| 실패 모드 | 없음 | **7일 미접근 시 캐시 삭제** | 개별 거래 유실 |

B 를 버린 이유가 핵심이다. Actions 캐시는 **7일간 접근이 없으면 삭제**되는데
우리 일정이 정확히 주 1회다. 실행이 하루만 밀려도 캐시가 사라지고 그 주는
5,600회 전량 재수집이 된다. **조용히 일어난다** — 이번에 고치려는 문제와 같은 종류다.

C 는 캐시 스키마 v3 의 개별 거래를 버린다. 화면에는 집계만 나가지만 신고가·
단지별 비교 같은 후속 기능의 재료가 그 개별 거래다. 지금 버리면 그때 5,500회
재수집부터 시작해야 한다.

A 는 **정본을 origin/main 하나로 확정한다.** 로컬과 러너가 각자의 캐시를 가지면
같은 (구·월·종) 조합도 마지막으로 받은 시점이 달라 결과가 갈린다 — 신고 지연 때문에
과거 월도 계속 채워지기 때문이다. 둘 다 정상 종료하고 어느 쪽이 맞는지 판단할
근거가 없다. 캐시가 git 에 있으면 이 갈라짐이 구조적으로 불가능해진다.

**편입 실측** — `git status` 는 커밋 전 0.02–0.03s, 커밋 후 0.04–0.05s 로 사실상
차이가 없다(파일 5,600개는 macOS 에서 큰 수가 아니다). `git add` 8.25s, 커밋 1.56s,
push 10.13s. `.git` 3.2M → 18M. **개선이 필요한 수준이 아니다.**

### 워크플로 (`.github/workflows/ingest.yml`)

매주 월 07:00 KST = `cron: '0 22 * * 0'` (UTC 일 22:00). `workflow_dispatch` 로
수동 실행 가능하며 `fresh` 입력으로 전량 재수집도 된다(평소에는 끈다).

- **수집이 실패하면 잡이 거기서 끝난다.** 뒤 단계가 없으므로 아무것도 커밋되지 않는다
- 최근 두 달만 갱신 — `ingest.mjs` 의 `recentYmds` 가 이미 처리한다. **코드 변경 없음**
- 키는 Secrets 에서 환경변수로만 넘긴다. 명령줄에 두지 않는다(`ps` 노출 방지).
  `ingest.mjs` 는 오류 메시지에 URL 을 넣지 않아 키가 로그에 남지 않는다
- `concurrency: ingest`, `cancel-in-progress: false` — 반쯤 쓰인 캐시를 남기지 않는다

**main 보호와 부딪히는 지점.** `main` 은 PR 필수이고 `GITHUB_TOKEN` 은 bypass
대상(Repository admin)이 아니라 직접 push 가 막힌다. Actions 를 bypass 에 넣으면
**`@claude` 에이전트도 main 에 직접 push 할 수 있게 된다.** 보호를 그대로 두고
규칙대로 PR 을 열어 병합했다 — 요구 승인이 0건이라 승인 없이 병합된다.

**병합만으로는 발행이 안 된다.** `GITHUB_TOKEN` 이 만든 push 로는 다른 워크플로가
트리거되지 않는다(`auto-pr.yml` 에서 겪은 것과 같은 함정). 실측으로 확인했다 —
run 31708472510 은 병합에 성공했으나 `pages.yml` 이 돌지 않았다. 병합 뒤
`gh workflow run pages.yml` 로 직접 부른다.

### 검증 (2026-08-13)

| 확인 | 결과 |
|---|---|
| 소요 | 잡 전체 **32초**, 수집 14초 |
| API 호출 | **200건 / 캐시 히트 5,400건 / 실패 0건** (apt·rh·sh·offi 각 50건) |
| 교차검증 | 전체 콤보 `totalCount` === 수집건수 일치 |
| 커밋 파일 | **117개** — 202607·202608 캐시 115개 + `dashboard.html` + `data.js` |
| `generatedAt` | 2026-08-12 → **2026-08-13** |
| 라이브 | `index.html` 200 (280,569 B), `data.js` 200 (477,037 B) |
| 버전 일치 | 라이브 html 의 `?v=158727e0` = 라이브 `data.js` sha256 앞 8자 = 로컬 파일 sha256 전체 일치 |
| 재실행(무변경) | PR 없음, 발행 없음 — `ingest.mjs` 가 `data.js` 갱신을 스스로 생략 |

### launchd 해제

검증 통과 후 내렸다. 되돌리려면:

    mv ~/Library/LaunchAgents/com.kuka.dashboard-ingest.plist.disabled \
       ~/Library/LaunchAgents/com.kuka.dashboard-ingest.plist
    launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kuka.dashboard-ingest.plist

plist 는 지우지 않고 `.disabled` 로 이름만 바꿨다(로그인 시 다시 뜨지 않게).
내용은 `run-ingest.sh` 를 매주 월 07:00 에 실행하는 것이 전부였다 —
`ProgramArguments: /Users/kuka/Project/dashboard/scripts/run-ingest.sh`,
`INGEST_NODE=/usr/local/bin/node`, `WorkingDirectory=/Users/kuka/Project`,
`StartCalendarInterval: Weekday 1 / Hour 7 / Minute 0`, `RunAtLoad: false`.

**로컬 실행 능력은 남긴다.** `run-ingest.sh` 와 `deploy.sh` 는 지우지 않았다.
다만 **수집은 러너가 한다. 로컬 실행은 디버깅용이며 커밋하지 않는다** — 양쪽이
같은 캐시 파일을 고치면 충돌하고, 정본이 둘로 갈린다.

### 5-1. 예약 실행과 알림 (2026-08-13)

**cron 재검증.** `'0 22 * * 0'` — GitHub 문서상 다섯 번째 칸은 요일이고 **0 = 일요일**,
기본 시간대는 **UTC** 다. 일요일 22:00 UTC + 9시간 = **월요일 07:00 KST**. 한국은
서머타임이 없어 연중 고정이다. 계산으로도 확인했다(2026-08-16 Sun 22:00 UTC
→ 2026-08-17 Mon 07:00 KST). **의도와 일치한다.**

예약 실행은 부하가 몰리면 수 시간 밀릴 수 있다. 아래 임계값이 그 지연을 오경보로
만들지 않도록 잡혀 있다.

**`freshness.yml` 임계값 10일 → 7일.** 10일은 너무 느슨했다. 근거:

| 상황 | 월 09:00 KST 나이 | 화 09:00 | 판정(상한 7, `age > 7` 이면 실패) |
|---|---|---|---|
| 정상 | 0 | 1 | 통과 |
| 수집이 몇 시간 지연 | **7** | 0 | **통과** — 오경보 없음 |
| 한 주를 통째로 거름 | 7 | **8** | **실패** — 화요일 아침에 잡는다 |

정상 운영에서 나이는 0~6일을 돌므로 7은 정상 범위 밖이다. 동시에 "지연됐지만
결국 성공"한 경우에 하루의 여유를 준다. 상한 10이면 같은 사고를 **그다음 주
금요일**에야 잡는다 — 4일을 앞당겼다.

**실패 알림은 API 로 조회할 수 없다.** REST Notifications API 에는 스레드 읽음·
구독 엔드포인트만 있고 **메일 수신 환경설정 엔드포인트가 없다.** 웹 UI 에서만 된다:
<https://github.com/settings/notifications> → **Actions** 섹션 →
"Notify for" 를 **Failed workflows only** 로. 이메일 체크가 켜져 있어야 메일이 온다.

**예약 실행의 알림은 cron 을 마지막으로 고친 사람에게 간다.** 문서에 명시돼 있다 —
"Notifications for scheduled workflows are sent to the user who initially created the
workflow. If a different user updates the cron syntax … subsequent notifications will
be sent to that user instead." 지금 `ingest.yml` 은 `byoneself4023-bit` 이 만들었으므로
알림도 그 계정으로 간다. **에이전트가 cron 줄을 고치면 알림이 새므로 cron 수정은
사람이 직접 한다.** 워크플로를 비활성화했다가 다시 켜면 수신자가 켠 사람으로 바뀐다.

**메일이 실제로 오는지 확인하는 법.** 일부러 실패를 만들 필요 없다. 2026-08-13 에
이미 **실패한 실행이 9건** 있다(에이전트 6건, 품질 게이트 2건, 백스톱 1건).
받은편지함에서 `notifications@github.com` 보낸이 + 제목에 `seoul-property-dashboard`
로 검색해 그날 메일이 있는지 보면 된다. 없으면 알림이 꺼져 있는 것이다.
수신 주소는 <https://github.com/settings/emails> 의 primary 주소다.

**공개 저장소의 60일 규칙.** 이 저장소는 **이미 공개다**(전환 완료). 문서: "In a
public repository, scheduled workflows are automatically disabled when no repository
activity has occurred in 60 days." 매주 수집 커밋이 들어오므로 평시에는 걸리지
않지만, **수집이 오래 멈춘 뒤에는 워크플로 자체가 꺼져 있을 수 있다** — 고쳐서
다시 돌리려 할 때 여기를 먼저 본다. Actions 탭에서 해당 워크플로를 열어
"Enable workflow" 를 누르면 된다. `gh workflow list` 의 상태가 `active` 가
아니면 그 경우다.

### 5-2. 조사 문서 이력 제거 (2026-08-13)

공개 상태에서 `docs/저장소-현황-조사.md`(70,590 B)가 노출돼 있었다. 본문은 없었지만
**사업 문서의 파일명·브랜드명·목차·크기**를 인용하고 있었다 — 기획안 파일명과 용량,
제품 브랜드명, 스택 제안서의 목차, `drive-docs/` 자산 목록. `drive-docs/`·`newsletter/`
를 이력에서 지운 것과 같은 원칙에 걸린다.

**자격증명 유출은 아니다.** 이력 blob 5,903개 전수 스캔에서 키 패턴 0건, `.env` 실제
값 3종 0건, Actions 로그 31개 전량에서도 0건, 작성자 전부 noreply 였다. 그래서
비공개 전환(= 무료 계정에서 Pages 중단 = 라이브 사망)이 아니라 **파일 제거 + 이력
재작성**을 택했다. 라이브는 끊기지 않았다.

    git filter-repo --path 'docs/저장소-현황-조사.md' --invert-paths --force
    git push --force origin main

백업 3겹(`~/Project-backup-20260813/pre-filter3/`) — 번들 14M, 전체 복사 237M,
파일 단독 69K. 파일은 로컬에 남기고 `.gitignore` 에 넣었다(작업 기록이라 참조가 필요).

**정리한 ref** — `claude/issue-1-*` 브랜치 3개 삭제, PR #3 닫음. 셋 다 재작성 이전
이력에 뿌리를 둬 병합이 불가능했고(main 대비 behind 28), 내용은 README 한 문단이었다.

#### 검사가 닿지 않아 두 번 오판했다

`git ls-files` 는 한글 파일명을 `"docs/\354\240\200…"` 로 인용한다. 그래서 첫
공개 전 점검에서 이 파일이 grep 에 걸리지 않았다. **`git log --name-only` 와
`git ls-tree` 도 똑같이 인용한다** — force push 후 원격 재검증에서 "어느 ref 에도
없음"이 나왔는데, 대조군(반드시 있어야 할 `docs/자동화-구축.md`)을 넣어보니 그것도
0건이었다. 검사가 애초에 닿지 않았던 것이다. 실제로는 브랜치 3개와 PR ref 4개에
파일이 그대로 있었다.

**규칙**: `git -c core.quotepath=false` 를 붙이거나 `git ls-files -z | tr '\0' '\n'`
을 쓴다. 그리고 **0건이 나오면 대조군으로 검사가 닿는지부터 확인한다.**
0건은 "깨끗하다"가 아니라 "아무것도 못 찾았다"이다.

#### `refs/pull/*` 는 남긴다 — 기준선

GitHub 의 PR ref 는 브랜치를 지워도, PR 을 닫아도 서버에 남는다.
`refs/pull/2/head`·`3/head`·`5/head` 세 개가 이 파일을 여전히 들고 있고,
`git fetch origin refs/pull/3/head` 로 접근된다. 지우려면 GitHub Support 에
요청해야 한다.

**요청하지 않기로 했다.** 노출된 것이 브랜드명·문서 파일명 수준이고 본문은 한 줄도
없다. 지원 요청 절차와 대기 시간이 그 값에 맞지 않는다.

**다음에 새면 이 기준선으로 판단한다 — 성격이 다르면 요청한다:**

| 새어나간 것 | 조치 |
|---|---|
| API 키·토큰·비밀번호 | **즉시 키 폐기·재발급이 먼저.** 그다음 Support 요청 |
| 사업 문서 **본문**, 특허 번호·제목, IR 수치, 녹취 | **Support 요청** |
| 개인정보(이름·연락처·주소) | **Support 요청** |
| 문서 파일명·제품 브랜드명·목차 | 이력 재작성까지. 요청하지 않음 ← 이번 건 |

---

## 남은 일

- **월요일 첫 예약 실행 확인 (2026-08-17)** — 수동 실행만 검증됐다. 그날
  `gh run list --workflow=ingest.yml` 에 `schedule` 이벤트 실행이 있는지,
  라이브 수집일이 그날짜로 바뀌었는지 본다. 안 돌았으면 워크플로가 `active` 인지부터
- **알림 설정 확인 (웹 전용)** — <https://github.com/settings/notifications> 의
  Actions 를 "Failed workflows only" 로. API 로는 조회도 변경도 안 된다
- **기존 배포 저장소 정리** — `seoul-property-dashboard-legacy` 는 옛 2026-07-13 데이터를
  그대로 서빙 중이다. 안내 페이지로 바꾸고 3개월 뒤 아카이브
- **`com.newsletter.*` LaunchAgent 2개** — 제거된 제품을 계속 실행하려다 실패하며
  로그를 쌓고 있다(`ui-server.log` 58MB). 이 저장소 밖의 문제지만 같이 정리하는 게 좋다
- **백로그 → 이슈 자동 생성** — 루틴으로 `docs/백로그.md` 를 읽어 이슈를 만드는 단계.
  앞의 것들이 안정된 뒤에 붙인다

## 실패 시 확인 경로

| 무엇 | 어디 |
|---|---|
| 라이브가 낡았는지 | Issues 탭 — `데이터-정지` 라벨이 붙은 이슈 |
| 신선도 감시 | `gh run list --workflow=freshness.yml` |
| 발행 실패 | `gh run list --workflow=pages.yml` → `gh run view <id> --log-failed` |
| 에이전트 실패 | `gh run list --workflow=claude.yml` → `gh run view <id> --log-failed` |
| 에이전트 오류 문구가 안 보일 때 | `claude.yml` 에 `show_full_output: true` 와 `--debug` 를 임시로 켠다 |
| 수집 실패 | `gh run list --workflow=ingest.yml` → `gh run view <id> --log-failed` |
| 예약이 꺼졌는지 | `gh workflow list` 에서 `데이터 수집` 이 `active` 인지 (공개 저장소 60일 규칙) |
| 옛 로컬 수집 로그 | `dashboard/logs/ingest-launchd.log` (launchd 는 2026-08-13 에 내렸다) |
| Stop 훅 검증 로그 | `.claude/.state/verify-<session>.log` |
| 이력 재작성 되돌리기 | `~/Project-backup-20260813/pre-filter2/Project-full-copy/` |

**키 값은 이 문서 어디에도 적지 않는다.**
